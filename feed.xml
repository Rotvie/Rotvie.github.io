<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://rotvie.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://rotvie.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-04-06T01:19:21+00:00</updated><id>https://rotvie.github.io/feed.xml</id><title type="html">Ricardo Huaman</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Decrypting Ciphers Using Markov Chain Monte Carlo</title><link href="https://rotvie.github.io/blog/2026/decrypting-ciphers-mcmc/" rel="alternate" type="text/html" title="Decrypting Ciphers Using Markov Chain Monte Carlo"/><published>2026-04-05T00:00:00+00:00</published><updated>2026-04-05T00:00:00+00:00</updated><id>https://rotvie.github.io/blog/2026/decrypting-ciphers-mcmc</id><content type="html" xml:base="https://rotvie.github.io/blog/2026/decrypting-ciphers-mcmc/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>In the <a href="https://rotvie.github.io/blog/2026/introduction-to-sampling-algorithms/">previous post</a>, we saw how sampling methods like rejection sampling and importance sampling break down in high dimensions — the probability of generating a useful sample drops exponentially as the space grows. Markov Chain Monte Carlo (MCMC) sidesteps this by constructing a sequence of correlated samples that gradually explore the target distribution, taking small, informed steps instead of hoping random guesses land in the right place.</p> <p>MCMC has applications across computational physics, biology, statistics, and machine learning <d-cite key="Robert_Casella_2004"></d-cite>. But here we’ll explore a particularly fun one: <strong>using MCMC to crack a substitution cipher</strong> — a coded message where each letter has been swapped for another <d-cite key="Rohde_2022"></d-cite>.</p> <p>To get there, we need two ingredients: Markov chains and Monte Carlo estimation. Let’s build them up.</p> <h2 id="markov-chains">Markov Chains</h2> <p>A Markov chain is a stochastic process that hops between states, where the probability of moving to the next state depends <em>only</em> on the current state — not on how you got there. This is the <strong>Markov property</strong>: the future is conditionally independent of the past given the present.</p> <p>Let’s make this concrete. Suppose we have three states — \(A\), \(B\), \(C\) — with transition probabilities that we can write compactly as a <strong>transition matrix</strong> \(P\), where entry \(P_{ij}\) is the probability of moving from state \(i\) to state \(j\):</p> \[P = \begin{pmatrix} 0.2 &amp; 0.5 &amp; 0.3 \\ 0.1 &amp; 0.6 &amp; 0.3 \\ 0.4 &amp; 0.2 &amp; 0.4 \end{pmatrix}\] <p>Each row sums to 1 — from any state, the probabilities of going somewhere (including staying put) must account for all possibilities.</p> <p>If we start in state \(A\) and simulate the chain by following these probabilities, we might get a sequence like: \(A \to B \to B \to C \to A \to B \to C \to C \to A \to B \to B \to \cdots\)</p> <h3 id="convergence-to-a-stationary-distribution">Convergence to a Stationary Distribution</h3> <p>Here’s the key property that makes Markov chains useful for sampling. If we run the chain long enough and count how often we visit each state, the proportions converge to a fixed distribution — the <strong>stationary distribution</strong> \(\pi\) — regardless of where we started.</p> <p>We can see this algebraically. Let \(\mathbf{s}_t\) be a row vector representing the probability of being in each state at time \(t\). Starting from state \(A\), we have \(\mathbf{s}_0 = (1, 0, 0)\). After one step:</p> \[\mathbf{s}_1 = \mathbf{s}_0 \cdot P = (0.2, 0.5, 0.3)\] <p>After two steps:</p> \[\mathbf{s}_2 = \mathbf{s}_1 \cdot P = (0.19, 0.46, 0.35)\] <p>If we keep multiplying, the distribution converges:</p> <table> <thead> <tr> <th>Step</th> <th>\(P(A)\)</th> <th>\(P(B)\)</th> <th>\(P(C)\)</th> </tr> </thead> <tbody> <tr> <td>0</td> <td>1.000</td> <td>0.000</td> <td>0.000</td> </tr> <tr> <td>1</td> <td>0.200</td> <td>0.500</td> <td>0.300</td> </tr> <tr> <td>2</td> <td>0.190</td> <td>0.460</td> <td>0.350</td> </tr> <tr> <td>5</td> <td>0.196</td> <td>0.390</td> <td>0.414</td> </tr> <tr> <td>10</td> <td>0.196</td> <td>0.392</td> <td>0.412</td> </tr> <tr> <td>50</td> <td>0.196</td> <td>0.392</td> <td>0.412</td> </tr> </tbody> </table> <p>The distribution stops changing — it has converged to the stationary distribution \(\pi \approx (0.196, 0.392, 0.412)\). This \(\pi\) satisfies:</p> \[\pi = \pi P\] <p>Meaning: if you’re distributed according to \(\pi\) and take one step, you stay distributed according to \(\pi\). The chain has reached equilibrium.</p> <p>What makes this remarkable is that <strong>the stationary distribution doesn’t depend on the starting state</strong>. Start from \(B\) or \(C\) instead of \(A\), and you’ll converge to the same \(\pi\). This is guaranteed (under mild conditions: the chain must be <strong>irreducible</strong> — you can get from any state to any other — and <strong>aperiodic</strong> — you don’t get trapped in deterministic cycles).</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/decrypting-ciphers-mcmc/mcmc-markov-chain-simulation-480.webp 480w,/assets/img/decrypting-ciphers-mcmc/mcmc-markov-chain-simulation-800.webp 800w,/assets/img/decrypting-ciphers-mcmc/mcmc-markov-chain-simulation-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/decrypting-ciphers-mcmc/mcmc-markov-chain-simulation.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Markov chain simulation" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>This is the property MCMC exploits: if we can construct a Markov chain whose stationary distribution is the target distribution we want to sample from, then running the chain long enough gives us samples from that target — even if we can’t sample from it directly.</p> <h2 id="monte-carlo-methods">Monte Carlo Methods</h2> <p>We covered Monte Carlo estimation in the <a href="https://rotvie.github.io/blog/2026/introduction-to-sampling-algorithms/">sampling algorithms post</a>, but let’s recap the essential idea. Monte Carlo methods approximate expectations by averaging over samples:</p> \[E_p[g(X)] \approx \frac{1}{N} \sum_{i=1}^{N} g(x_i), \quad x_i \sim p(x)\] <p>The Law of Large Numbers guarantees this converges to the true expectation as \(N \to \infty\). The key requirement is that the samples \(x_i\) come from the distribution \(p(x)\).</p> <p>But what if we can’t sample from \(p\) directly? That’s exactly the scenario where MCMC shines. Instead of drawing independent samples from \(p\), we construct a Markov chain whose stationary distribution <em>is</em> \(p\), run it until convergence (the “burn-in” period), and then treat the subsequent states as (correlated) samples from \(p\). The combination of <strong>M</strong>arkov <strong>C</strong>hains (to generate samples) and <strong>M</strong>onte <strong>C</strong>arlo (to estimate quantities from those samples) gives MCMC its name.</p> <h2 id="the-substitution-cipher">The Substitution Cipher</h2> <p>The substitution cipher is one of the simplest cryptographic methods. It works by swapping each letter in the alphabet with another according to a fixed mapping.</p> <p>A simple example is a <strong>shift cipher</strong>, where we shift the alphabet by a fixed number of positions. A shift of 3 would give:</p> <figure class="highlight"><pre><code class="language-text" data-lang="text">Alfabeto original: abcdefghijklmnopqrstuvwxyz
Alfabeto cifrado: defghijklmnopqrstuvwxyzabc</code></pre></figure> <p>With this cipher, the word <code class="language-plaintext highlighter-rouge">hola</code> would map to:</p> <ul> <li><code class="language-plaintext highlighter-rouge">h -&gt; k</code></li> <li><code class="language-plaintext highlighter-rouge">o -&gt; r</code></li> <li><code class="language-plaintext highlighter-rouge">l -&gt; o</code></li> <li><code class="language-plaintext highlighter-rouge">a -&gt; d</code></li> </ul> <p>resulting in the ciphertext <code class="language-plaintext highlighter-rouge">krod</code>.</p> <blockquote> <p><strong>Terminology.</strong> <strong>Plaintext</strong> refers to the text you input to the cipher (<code class="language-plaintext highlighter-rouge">hola</code>) and <strong>ciphertext</strong> refers to the output (<code class="language-plaintext highlighter-rouge">krod</code>).</p> </blockquote> <p>More generally, we can create an arbitrary substitution cipher by permuting the letters of the alphabet — not just shifting them. Each cipher is a permutation \(\sigma\) of the character set. Encryption applies \(\sigma\) letter by letter; decryption applies the inverse \(\sigma^{-1}\).</p> <p>For our problem, we’ll work with <strong>normalized Spanish text</strong>: 26 lowercase letters plus space (no <code class="language-plaintext highlighter-rouge">ñ</code>, no accents, no punctuation) — 27 characters total. Here’s the plaintext we’ll use, from Borges’ <em>Ficciones</em> <d-cite key="Borges_Ficciones"></d-cite>:</p> <figure class="highlight"><pre><code class="language-text" data-lang="text">he dicho que los hombres de ese planeta conciben el universo como una serie
de procesos mentales que no se desenvuelven en el espacio sino de modo
sucesivo en el tiempo</code></pre></figure> <p>Now suppose someone intercepts the ciphertext but doesn’t know the cipher. How would they decode it?</p> <h3 id="why-not-brute-force">Why Not Brute Force?</h3> <p>A first idea: try all possible ciphers until we find the right one. How many are there? Each cipher is a permutation of our 27-character set, so there are \(27!\) possibilities:</p> \[27! \approx 1.09 \times 10^{28}\] <p>That’s an astronomically large number. As a thought experiment, let’s assume we have access to the world’s fastest supercomputer, reported to compute \(\sim 1.1 \times 10^{18}\) operations per second. Although checking a cipher would take more than a single operation, let’s assume we can check one per operation to see if this approach is even feasible:</p> \[\frac{1.09 \times 10^{28} \text{ ciphers}}{1.1 \times 10^{18} \text{ ciphers/sec}} \approx 9.9 \times 10^{9} \text{ seconds} \approx 314 \text{ years}\] <p>Even with extremely generous assumptions, brute force is hopeless. We need a smarter approach — one that seeks out the likely ciphers and ignores the rest. This is what the Metropolis algorithm does <d-cite key="Metropolis_1953"></d-cite>.</p> <h2 id="defining-a-spanish-similarity-score">Defining a “Spanish-Similarity Score”</h2> <p>Before we can run the Metropolis algorithm, we need a way to measure how similar an arbitrary text looks to Spanish. Even without speaking the language, you could probably tell that <code class="language-plaintext highlighter-rouge">krod</code> doesn’t look like Spanish — most real words don’t have that letter pattern.</p> <p>The idea: real Spanish has strong statistical regularities in which characters tend to appear next to each other. The pair <code class="language-plaintext highlighter-rouge">de</code> is extremely common; <code class="language-plaintext highlighter-rouge">qz</code> almost never occurs. We can exploit this by looking at <strong>bigram frequencies</strong> — how often each pair of consecutive characters appears in a large Spanish corpus.</p> <h3 id="building-a-bigram-matrix-with-pytorch">Building a Bigram Matrix with PyTorch</h3> <p>Let’s build a bigram frequency matrix from a reference Spanish text. We’ll use a corpus of Borges’ collected works <d-cite key="Borges_Corpus"></d-cite>, cleaned to contain only our 27 characters.</p> <p>The approach follows the character-level language model pattern: we create a count matrix \(N\) (27 characters + a special boundary token <code class="language-plaintext highlighter-rouge">.</code>) where \(N_{ij}\) counts how many times character \(j\) follows character \(i\) in the corpus. We count bigrams over the continuous text (including spaces), with the boundary token only at the start and end of the corpus.</p> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="kn">import</span> <span class="n">torch</span>

<span class="c1"># Load and clean the reference corpus
</span><span class="k">with</span> <span class="nf">open</span><span class="p">(</span><span class="sh">'</span><span class="s">corpus.txt</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">r</span><span class="sh">'</span><span class="p">,</span> <span class="n">encoding</span><span class="o">=</span><span class="sh">'</span><span class="s">utf-8</span><span class="sh">'</span><span class="p">)</span> <span class="k">as</span> <span class="n">f</span><span class="p">:</span>
    <span class="n">text</span> <span class="o">=</span> <span class="n">f</span><span class="p">.</span><span class="nf">read</span><span class="p">()</span>

<span class="c1"># Normalize: lowercase, remove accents, ñ -&gt; n, keep only a-z and space
</span><span class="kn">import</span> <span class="n">unicodedata</span>
<span class="k">def</span> <span class="nf">normalize_spanish</span><span class="p">(</span><span class="n">s</span><span class="p">):</span>
    <span class="n">s</span> <span class="o">=</span> <span class="n">s</span><span class="p">.</span><span class="nf">lower</span><span class="p">()</span>
    <span class="n">s</span> <span class="o">=</span> <span class="n">s</span><span class="p">.</span><span class="nf">replace</span><span class="p">(</span><span class="sh">'</span><span class="se">\n</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s"> </span><span class="sh">'</span><span class="p">)</span>  <span class="c1"># treat newlines as word boundaries
</span>    <span class="n">s</span> <span class="o">=</span> <span class="n">unicodedata</span><span class="p">.</span><span class="nf">normalize</span><span class="p">(</span><span class="sh">'</span><span class="s">NFD</span><span class="sh">'</span><span class="p">,</span> <span class="n">s</span><span class="p">)</span>
    <span class="n">s</span> <span class="o">=</span> <span class="sh">''</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="n">c</span> <span class="k">for</span> <span class="n">c</span> <span class="ow">in</span> <span class="n">s</span> <span class="k">if</span> <span class="n">unicodedata</span><span class="p">.</span><span class="nf">category</span><span class="p">(</span><span class="n">c</span><span class="p">)</span> <span class="o">!=</span> <span class="sh">'</span><span class="s">Mn</span><span class="sh">'</span><span class="p">)</span>  <span class="c1"># strip accents
</span>    <span class="n">s</span> <span class="o">=</span> <span class="n">s</span><span class="p">.</span><span class="nf">replace</span><span class="p">(</span><span class="sh">'</span><span class="s">ñ</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">n</span><span class="sh">'</span><span class="p">)</span>
    <span class="n">s</span> <span class="o">=</span> <span class="sh">''</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="n">c</span> <span class="k">if</span> <span class="n">c</span> <span class="ow">in</span> <span class="sh">'</span><span class="s">abcdefghijklmnopqrstuvwxyz </span><span class="sh">'</span> <span class="k">else</span> <span class="sh">''</span> <span class="k">for</span> <span class="n">c</span> <span class="ow">in</span> <span class="n">s</span><span class="p">)</span>
    <span class="n">s</span> <span class="o">=</span> <span class="sh">'</span><span class="s"> </span><span class="sh">'</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="n">s</span><span class="p">.</span><span class="nf">split</span><span class="p">())</span>  <span class="c1"># collapse multiple spaces
</span>    <span class="k">return</span> <span class="n">s</span>

<span class="n">text</span> <span class="o">=</span> <span class="nf">normalize_spanish</span><span class="p">(</span><span class="n">text</span><span class="p">)</span>

<span class="c1"># Character-to-index mapping: '.' = 0 (boundary), ' ' = 1, 'a' = 2, ..., 'z' = 27
</span><span class="n">words</span> <span class="o">=</span> <span class="n">text</span><span class="p">.</span><span class="nf">split</span><span class="p">()</span>
<span class="n">chars</span> <span class="o">=</span> <span class="nf">sorted</span><span class="p">(</span><span class="nf">list</span><span class="p">(</span><span class="nf">set</span><span class="p">(</span><span class="sh">'</span><span class="s"> </span><span class="sh">'</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="n">words</span><span class="p">))))</span>
<span class="n">stoi</span> <span class="o">=</span> <span class="p">{</span><span class="n">s</span><span class="p">:</span> <span class="n">i</span><span class="o">+</span><span class="mi">1</span> <span class="k">for</span> <span class="n">i</span><span class="p">,</span> <span class="n">s</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">chars</span><span class="p">)}</span>
<span class="n">stoi</span><span class="p">[</span><span class="sh">'</span><span class="s">.</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="mi">0</span>
<span class="n">itos</span> <span class="o">=</span> <span class="p">{</span><span class="n">i</span><span class="p">:</span> <span class="n">s</span> <span class="k">for</span> <span class="n">s</span><span class="p">,</span> <span class="n">i</span> <span class="ow">in</span> <span class="n">stoi</span><span class="p">.</span><span class="nf">items</span><span class="p">()}</span>

<span class="c1"># Build bigram count matrix over continuous text
</span><span class="n">n_tokens</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">stoi</span><span class="p">)</span>
<span class="n">N</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">((</span><span class="n">n_tokens</span><span class="p">,</span> <span class="n">n_tokens</span><span class="p">),</span> <span class="n">dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="n">int32</span><span class="p">)</span>

<span class="n">chs</span> <span class="o">=</span> <span class="p">[</span><span class="sh">'</span><span class="s">.</span><span class="sh">'</span><span class="p">]</span> <span class="o">+</span> <span class="nf">list</span><span class="p">(</span><span class="n">text</span><span class="p">)</span> <span class="o">+</span> <span class="p">[</span><span class="sh">'</span><span class="s">.</span><span class="sh">'</span><span class="p">]</span>
<span class="k">for</span> <span class="n">ch1</span><span class="p">,</span> <span class="n">ch2</span> <span class="ow">in</span> <span class="nf">zip</span><span class="p">(</span><span class="n">chs</span><span class="p">,</span> <span class="n">chs</span><span class="p">[</span><span class="mi">1</span><span class="p">:]):</span>
    <span class="n">ix1</span> <span class="o">=</span> <span class="n">stoi</span><span class="p">[</span><span class="n">ch1</span><span class="p">]</span>
    <span class="n">ix2</span> <span class="o">=</span> <span class="n">stoi</span><span class="p">[</span><span class="n">ch2</span><span class="p">]</span>
    <span class="n">N</span><span class="p">[</span><span class="n">ix1</span><span class="p">,</span> <span class="n">ix2</span><span class="p">]</span> <span class="o">+=</span> <span class="mi">1</span></code></pre></figure> <p>We can visualize this matrix as a heatmap — brighter cells indicate more frequent bigrams:</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/decrypting-ciphers-mcmc/mcmc-bigram-heatmap-480.webp 480w,/assets/img/decrypting-ciphers-mcmc/mcmc-bigram-heatmap-800.webp 800w,/assets/img/decrypting-ciphers-mcmc/mcmc-bigram-heatmap-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/decrypting-ciphers-mcmc/mcmc-bigram-heatmap.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Bigram frequency heatmap" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Now we convert counts to probabilities. To avoid zero probabilities for unseen bigrams (which would make the log-likelihood \(-\infty\)), we add 1 to every count before normalizing — this is <strong>Laplace smoothing</strong>:</p> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="c1"># Convert to probability matrix with Laplace smoothing
</span><span class="n">P</span> <span class="o">=</span> <span class="p">(</span><span class="n">N</span> <span class="o">+</span> <span class="mi">1</span><span class="p">).</span><span class="nf">float</span><span class="p">()</span>
<span class="n">P</span> <span class="o">/=</span> <span class="n">P</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="n">keepdims</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span></code></pre></figure> <p>Each row of \(P\) now sums to 1 and represents a probability distribution: \(P_{ij}\) is the estimated probability that character \(j\) follows character \(i\) in Spanish.</p> <h3 id="scoring-a-text">Scoring a Text</h3> <p>Given a candidate decryption \(d = d_1 d_2 \dots d_n\), we can measure how “Spanish-like” it looks by computing the product of all consecutive bigram probabilities:</p> \[\text{sim}(d) = P(d_1, d_2) \times P(d_2, d_3) \times \cdots \times P(d_{n-1}, d_n)\] <p>This product will be large if the text follows typical Spanish letter patterns (lots of common bigrams) and tiny if it doesn’t (full of rare bigrams).</p> <p>However, multiplying hundreds of small probabilities together quickly underflows to zero. We work on the <strong>log scale</strong> instead, turning products into sums:</p> \[\text{Score}(d) = \sum_{i=1}^{n-1} \log P(d_i, d_{i+1})\] <p>Higher scores mean more Spanish-like text. This is our <strong>log-likelihood</strong>.</p> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="k">def</span> <span class="nf">score_text</span><span class="p">(</span><span class="n">text</span><span class="p">,</span> <span class="n">P</span><span class="p">,</span> <span class="n">stoi</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">Compute log-likelihood of a text under the bigram model.</span><span class="sh">"""</span>
    <span class="n">log_prob</span> <span class="o">=</span> <span class="mf">0.0</span>
    <span class="k">for</span> <span class="n">ch1</span><span class="p">,</span> <span class="n">ch2</span> <span class="ow">in</span> <span class="nf">zip</span><span class="p">(</span><span class="n">text</span><span class="p">,</span> <span class="n">text</span><span class="p">[</span><span class="mi">1</span><span class="p">:]):</span>
        <span class="n">ix1</span> <span class="o">=</span> <span class="n">stoi</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="n">ch1</span><span class="p">,</span> <span class="mi">0</span><span class="p">)</span>
        <span class="n">ix2</span> <span class="o">=</span> <span class="n">stoi</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="n">ch2</span><span class="p">,</span> <span class="mi">0</span><span class="p">)</span>
        <span class="n">log_prob</span> <span class="o">+=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">P</span><span class="p">[</span><span class="n">ix1</span><span class="p">,</span> <span class="n">ix2</span><span class="p">]).</span><span class="nf">item</span><span class="p">()</span>
    <span class="k">return</span> <span class="n">log_prob</span></code></pre></figure> <p>Let’s verify that the score can differentiate Spanish from gibberish:</p> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="o">&gt;&gt;&gt;</span> <span class="nf">score_text</span><span class="p">(</span><span class="sh">"</span><span class="s">esto es texto en espanol</span><span class="sh">"</span><span class="p">,</span> <span class="n">P</span><span class="p">,</span> <span class="n">stoi</span><span class="p">)</span>
<span class="o">-</span><span class="mf">78.43</span>

<span class="o">&gt;&gt;&gt;</span> <span class="nf">score_text</span><span class="p">(</span><span class="sh">"</span><span class="s">fghr gh wghdfrf etfs xqzk</span><span class="sh">"</span><span class="p">,</span> <span class="n">P</span><span class="p">,</span> <span class="n">stoi</span><span class="p">)</span>
<span class="o">-</span><span class="mf">112.67</span></code></pre></figure> <p>The Spanish text gets a much higher (less negative) score — exactly what we want.</p> <h2 id="the-metropolis-algorithm">The Metropolis Algorithm</h2> <p>Now we have everything we need. The Metropolis algorithm constructs a Markov chain over the space of all \(27!\) possible ciphers, biased to visit ciphers that produce Spanish-like decryptions more often <d-cite key="Metropolis_1953"></d-cite>.</p> <p>The algorithm works like this. Start with a random cipher. Then repeat:</p> <ol> <li> <p><strong>Propose</strong> a new cipher by swapping two characters in the current cipher at random. This small change is our proposal — it produces a cipher that’s “close” to the current one.</p> </li> <li> <p><strong>Score</strong> both: decode the ciphertext with the proposed cipher and the current cipher, and compute the log-likelihood of each decoded text.</p> </li> <li> <p><strong>Accept or reject</strong>: compute the acceptance probability \(\alpha = \min\left(1, \; e^{\text{Score}(\text{proposed}) - \text{Score}(\text{current})}\right)\) If the proposed decryption looks more like Spanish (\(\alpha \geq 1\)), always accept it. If it looks less like Spanish, accept it with probability \(\alpha\). This occasional acceptance of worse states prevents the chain from getting stuck in local optima.</p> </li> </ol> <p>Notice that we only need the <em>ratio</em> of scores (equivalently, the <em>difference</em> of log-scores), so any normalizing constants cancel. This is the key property that makes MCMC practical — we never need to compute the intractable normalization over \(27!\) permutations.</p> <p>Since our proposal is symmetric (swapping letters \(i \leftrightarrow j\) is the same move as swapping \(j \leftrightarrow i\)), this is the original <strong>Metropolis</strong> algorithm — the special case of Metropolis-Hastings where the proposal cancels from the acceptance ratio <d-cite key="Hastings_1970"></d-cite>.</p> <h3 id="implementation">Implementation</h3> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="kn">import</span> <span class="n">random</span>

<span class="c1"># The plaintext (we pretend we don't know this)
</span><span class="n">plaintext</span> <span class="o">=</span> <span class="p">(</span><span class="sh">"</span><span class="s">he dicho que los hombres de ese planeta conciben el universo </span><span class="sh">"</span>
             <span class="sh">"</span><span class="s">como una serie de procesos mentales que no se desenvuelven </span><span class="sh">"</span>
             <span class="sh">"</span><span class="s">en el espacio sino de modo sucesivo en el tiempo</span><span class="sh">"</span><span class="p">)</span>

<span class="c1"># Create the alphabet: a-z + space
</span><span class="n">alphabet</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="sh">'</span><span class="s">abcdefghijklmnopqrstuvwxyz </span><span class="sh">'</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">generate_cipher</span><span class="p">():</span>
    <span class="sh">"""</span><span class="s">Generate a random permutation of the alphabet.</span><span class="sh">"""</span>
    <span class="n">cipher</span> <span class="o">=</span> <span class="n">alphabet</span><span class="p">.</span><span class="nf">copy</span><span class="p">()</span>
    <span class="n">random</span><span class="p">.</span><span class="nf">shuffle</span><span class="p">(</span><span class="n">cipher</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">cipher</span>

<span class="k">def</span> <span class="nf">encode</span><span class="p">(</span><span class="n">text</span><span class="p">,</span> <span class="n">cipher</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">Encode text using a cipher (permutation).</span><span class="sh">"""</span>
    <span class="n">mapping</span> <span class="o">=</span> <span class="p">{</span><span class="n">original</span><span class="p">:</span> <span class="n">coded</span> <span class="k">for</span> <span class="n">original</span><span class="p">,</span> <span class="n">coded</span> <span class="ow">in</span> <span class="nf">zip</span><span class="p">(</span><span class="n">alphabet</span><span class="p">,</span> <span class="n">cipher</span><span class="p">)}</span>
    <span class="k">return</span> <span class="sh">''</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="n">mapping</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="n">c</span><span class="p">,</span> <span class="n">c</span><span class="p">)</span> <span class="k">for</span> <span class="n">c</span> <span class="ow">in</span> <span class="n">text</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">decode</span><span class="p">(</span><span class="n">ciphertext</span><span class="p">,</span> <span class="n">cipher</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">Decode ciphertext using a cipher (inverse permutation).</span><span class="sh">"""</span>
    <span class="n">mapping</span> <span class="o">=</span> <span class="p">{</span><span class="n">coded</span><span class="p">:</span> <span class="n">original</span> <span class="k">for</span> <span class="n">original</span><span class="p">,</span> <span class="n">coded</span> <span class="ow">in</span> <span class="nf">zip</span><span class="p">(</span><span class="n">alphabet</span><span class="p">,</span> <span class="n">cipher</span><span class="p">)}</span>
    <span class="k">return</span> <span class="sh">''</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="n">mapping</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="n">c</span><span class="p">,</span> <span class="n">c</span><span class="p">)</span> <span class="k">for</span> <span class="n">c</span> <span class="ow">in</span> <span class="n">ciphertext</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">swap_two</span><span class="p">(</span><span class="n">cipher</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">Propose a new cipher by swapping two random positions.</span><span class="sh">"""</span>
    <span class="n">new_cipher</span> <span class="o">=</span> <span class="n">cipher</span><span class="p">.</span><span class="nf">copy</span><span class="p">()</span>
    <span class="n">i</span><span class="p">,</span> <span class="n">j</span> <span class="o">=</span> <span class="n">random</span><span class="p">.</span><span class="nf">sample</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">cipher</span><span class="p">)),</span> <span class="mi">2</span><span class="p">)</span>
    <span class="n">new_cipher</span><span class="p">[</span><span class="n">i</span><span class="p">],</span> <span class="n">new_cipher</span><span class="p">[</span><span class="n">j</span><span class="p">]</span> <span class="o">=</span> <span class="n">new_cipher</span><span class="p">[</span><span class="n">j</span><span class="p">],</span> <span class="n">new_cipher</span><span class="p">[</span><span class="n">i</span><span class="p">]</span>
    <span class="k">return</span> <span class="n">new_cipher</span>

<span class="c1"># Generate the true cipher and produce the ciphertext
</span><span class="n">true_cipher</span> <span class="o">=</span> <span class="nf">generate_cipher</span><span class="p">()</span>
<span class="n">ciphertext</span> <span class="o">=</span> <span class="nf">encode</span><span class="p">(</span><span class="n">plaintext</span><span class="p">,</span> <span class="n">true_cipher</span><span class="p">)</span>

<span class="c1"># Start with a random cipher
</span><span class="n">current_cipher</span> <span class="o">=</span> <span class="nf">generate_cipher</span><span class="p">()</span>
<span class="n">current_score</span> <span class="o">=</span> <span class="nf">score_text</span><span class="p">(</span><span class="nf">decode</span><span class="p">(</span><span class="n">ciphertext</span><span class="p">,</span> <span class="n">current_cipher</span><span class="p">),</span> <span class="n">P</span><span class="p">,</span> <span class="n">stoi</span><span class="p">)</span>

<span class="c1"># Run the Metropolis algorithm
</span><span class="k">for</span> <span class="n">iteration</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">50000</span><span class="p">):</span>
    <span class="c1"># Propose a new cipher
</span>    <span class="n">proposed_cipher</span> <span class="o">=</span> <span class="nf">swap_two</span><span class="p">(</span><span class="n">current_cipher</span><span class="p">)</span>

    <span class="c1"># Score the proposed decryption
</span>    <span class="n">proposed_text</span> <span class="o">=</span> <span class="nf">decode</span><span class="p">(</span><span class="n">ciphertext</span><span class="p">,</span> <span class="n">proposed_cipher</span><span class="p">)</span>
    <span class="n">proposed_score</span> <span class="o">=</span> <span class="nf">score_text</span><span class="p">(</span><span class="n">proposed_text</span><span class="p">,</span> <span class="n">P</span><span class="p">,</span> <span class="n">stoi</span><span class="p">)</span>

    <span class="c1"># Acceptance probability (on log scale: difference of scores)
</span>    <span class="n">log_alpha</span> <span class="o">=</span> <span class="n">proposed_score</span> <span class="o">-</span> <span class="n">current_score</span>

    <span class="c1"># Accept or reject
</span>    <span class="k">if</span> <span class="n">log_alpha</span> <span class="o">&gt;=</span> <span class="mi">0</span> <span class="ow">or</span> <span class="n">random</span><span class="p">.</span><span class="nf">random</span><span class="p">()</span> <span class="o">&lt;</span> <span class="n">math</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">log_alpha</span><span class="p">):</span>
        <span class="n">current_cipher</span> <span class="o">=</span> <span class="n">proposed_cipher</span>
        <span class="n">current_score</span> <span class="o">=</span> <span class="n">proposed_score</span>

        <span class="k">if</span> <span class="n">iteration</span> <span class="o">%</span> <span class="mi">500</span> <span class="o">==</span> <span class="mi">0</span><span class="p">:</span>
            <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Iteration </span><span class="si">{</span><span class="n">iteration</span><span class="si">}</span><span class="s">: </span><span class="si">{</span><span class="n">proposed_text</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span></code></pre></figure> <p>Here’s what the output looks like as the chain converges:</p> <figure class="highlight"><pre><code class="language-text" data-lang="text">Iteration 0: dxptfsmihqxtiehdienbxehtxexexokglxfghsiglfxxtixqlfaxuehxshenxqgn...
Iteration 500: je disno wue cos nombtes de ele praneta sonsiben er univelso...
Iteration 2000: he dicho que los hombres de ese planeta conciben el universo...</code></pre></figure> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/decrypting-ciphers-mcmc/mcmc-cipher-convergence-480.webp 480w,/assets/img/decrypting-ciphers-mcmc/mcmc-cipher-convergence-800.webp 800w,/assets/img/decrypting-ciphers-mcmc/mcmc-cipher-convergence-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/decrypting-ciphers-mcmc/mcmc-cipher-convergence.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Cipher convergence" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>The chain starts with gibberish and within a few thousand iterations, the Borges quote emerges. Each accepted swap nudges the decryption toward more Spanish-like text. The occasional acceptance of worse proposals allows the chain to escape local optima — configurations where a few letters are correct but swapping any single pair makes things worse before they get better.</p> <h3 id="the-burn-in-period">The Burn-In Period</h3> <p>The chain starts in a random state that is almost certainly far from the target distribution. The initial samples are not representative — the chain needs time to “burn in” and reach the high-probability region. We can see this by plotting the log-likelihood score over iterations: it rises sharply at first, then levels off once the chain has found the neighborhood of the true cipher.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/decrypting-ciphers-mcmc/mcmc-burnin-score-480.webp 480w,/assets/img/decrypting-ciphers-mcmc/mcmc-burnin-score-800.webp 800w,/assets/img/decrypting-ciphers-mcmc/mcmc-burnin-score-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/decrypting-ciphers-mcmc/mcmc-burnin-score.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Burn-in score trajectory" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>In practice, we discard the first several thousand samples (the burn-in period) and only use subsequent ones for inference. For our cipher problem, we’re using MCMC for optimization rather than sampling — we just want the highest-scoring cipher found — so burn-in is less of a concern. But it’s worth noting that this same machinery, when used for Bayesian inference, requires careful attention to convergence diagnostics.</p> <h3 id="practical-notes">Practical Notes</h3> <p>A few things that affect convergence:</p> <ul> <li><strong>Text length matters.</strong> Longer texts contain more bigram information, making the score function sharper — the algorithm converges faster. Our Borges quote (~180 characters) is on the short side; a full paragraph would converge in fewer iterations.</li> <li><strong>Random restarts help.</strong> Since the cipher space has many local optima, some initial ciphers lead to chains that get stuck. If the chain hasn’t converged after 20,000 iterations, restarting with a new random cipher is a reasonable strategy for optimization (though not for sampling applications of MCMC).</li> <li><strong>The proposal matters.</strong> Swapping two letters is a good proposal because it makes a small change — most of the decoded text stays the same. If we instead proposed completely random permutations, the acceptance rate would be near zero and the chain would never move.</li> </ul> <h2 id="summary">Summary</h2> <p>We took two ideas — Markov chains converging to a stationary distribution, and Monte Carlo estimation via sampling — and combined them into MCMC, a method that can sample from distributions over spaces far too large to enumerate.</p> <p>The cipher decryption example illustrates the core mechanism:</p> <ol> <li>We defined a <strong>score function</strong> (bigram log-likelihood) that measures how Spanish-like a decoded text looks</li> <li>This score induces a <strong>probability distribution</strong> over the \(27!\) possible ciphers — higher scores mean more probable</li> <li>The <strong>Metropolis algorithm</strong> explores this space efficiently by proposing small changes (letter swaps), accepting improvements, and occasionally accepting worse states to avoid getting trapped</li> </ol> <table> <thead> <tr> <th>Concept</th> <th>Role in cipher decryption</th> </tr> </thead> <tbody> <tr> <td>Markov chain</td> <td>Sequence of ciphers, each obtained by swapping two letters in the previous one</td> </tr> <tr> <td>Stationary distribution</td> <td>The distribution over ciphers proportional to Spanish-likeness score</td> </tr> <tr> <td>Monte Carlo</td> <td>Using the chain’s samples to find the most likely decryption</td> </tr> <tr> <td>Metropolis algorithm</td> <td>The accept/reject rule that ensures the chain converges to the right distribution</td> </tr> </tbody> </table> <p>This is the same machinery behind Bayesian inference in high-dimensional models, protein folding simulations, and probabilistic programming — just with different state spaces and score functions.</p>]]></content><author><name>Ricardo Huaman</name></author><category term="MCMC,"/><category term="statistics,"/><category term="probability,"/><category term="cryptography"/><summary type="html"><![CDATA[Combining Markov chains and Monte Carlo estimation to crack a substitution cipher — from bigram statistics to the Metropolis algorithm]]></summary></entry><entry><title type="html">Introduction to Flow Matching with Robotics</title><link href="https://rotvie.github.io/blog/2026/flow-matching-robotics/" rel="alternate" type="text/html" title="Introduction to Flow Matching with Robotics"/><published>2026-03-15T00:00:00+00:00</published><updated>2026-03-15T00:00:00+00:00</updated><id>https://rotvie.github.io/blog/2026/flow-matching-robotics</id><content type="html" xml:base="https://rotvie.github.io/blog/2026/flow-matching-robotics/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>Most tutorials on flow matching demonstrate it on toy 2D distributions — moons, circles, spirals. This post takes a different route. We’ll use flow matching to solve a problem from robotics: <strong>generating collision-free configurations for a robot arm</strong>.</p> <p>The idea is simple. A 2-link planar arm operates in a workspace cluttered with obstacles. Some joint angle combinations cause the arm to collide; others are safe. The set of all safe configurations forms a complex, non-convex region in <strong>configuration space</strong> (C-space). We want a generative model that produces samples exclusively from this free region — without ever having to solve the collision geometry at test time.</p> <p>Flow matching gives us exactly the tool for this. It learns a velocity field that transports Gaussian noise into the target distribution, and at inference we just integrate an ODE. No rejection sampling, no iterative denoising with hundreds of steps — just a clean forward pass through a learned vector field.</p> <p>This post covers the theory behind flow matching, builds intuition for why the objective simplifies the way it does, and walks through a complete implementation — from data generation to evaluation.</p> <h2 id="the-problem-sampling-from-configuration-space">The Problem: Sampling from Configuration Space</h2> <p>A planar robot arm has two revolute joints with angles \((\theta_1, \theta_2) \in [-\pi, \pi]^2\) and link lengths \(L_1 = 1.5\), \(L_2 = 1.0\). The workspace contains three circular obstacles.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/flow-matching-robotics/fm-workspace-cspace-480.webp 480w,/assets/img/flow-matching-robotics/fm-workspace-cspace-800.webp 800w,/assets/img/flow-matching-robotics/fm-workspace-cspace-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/flow-matching-robotics/fm-workspace-cspace.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Workspace and C-space" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Left: the 2-link arm in its workspace with three circular obstacles. Right: the corresponding configuration space. Green dots are collision-free configurations; gray regions are obstacle zones. The free C-space has a complex, non-convex structure that makes naive sampling inefficient. </div> <p>The left panel shows the physical arm and obstacles. The right panel shows what happens when we map the obstacle boundaries into joint angle space: the free region fragments into irregular channels separated by curved obstacle zones. If we sampled \((\theta_1, \theta_2)\) uniformly at random, roughly 35% of configurations would collide — a significant waste if we need valid configurations for motion planning or policy learning.</p> <p>The goal is to learn a generative model whose samples concentrate in the free (white) regions of C-space.</p> <h2 id="flow-matching-the-core-idea">Flow Matching: The Core Idea</h2> <p>Flow matching constructs a time-dependent velocity field \(v_\theta(x_t, t)\) that defines an ordinary differential equation (ODE):</p> \[\frac{dx_t}{dt} = v_\theta(x_t, t), \qquad t \in [0, 1]\] <p>At \(t = 0\), we start from a simple distribution we can sample from — a standard Gaussian. At \(t = 1\), the ODE should have transported those samples into the target distribution — our collision-free C-space. The velocity field \(v_\theta\) is a neural network trained to make this transport happen.</p> <p>The question is: what should the network learn to predict?</p> <h3 id="the-interpolation-trick">The Interpolation Trick</h3> <p>The key idea in flow matching is to define straight-line paths between noise and data <d-cite key="Lipman_2023"></d-cite>. Given a noise sample \(x_0 \sim \mathcal{N}(0, I)\) and a data sample \(x_1\) from the target, the interpolation is:</p> \[x_t = (1 - t)\, x_0 + t\, x_1\] <p>Taking the time derivative:</p> \[\frac{dx_t}{dt} = x_1 - x_0\] <p>This is the <strong>conditional velocity</strong> — the constant-speed straight line from noise to data. At every point in time, the “true” velocity that moves a specific noise sample toward a specific data point is just \(x_1 - x_0\).</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/flow-matching-robotics/fm-conditional-paths-480.webp 480w,/assets/img/flow-matching-robotics/fm-conditional-paths-800.webp 800w,/assets/img/flow-matching-robotics/fm-conditional-paths-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/flow-matching-robotics/fm-conditional-paths.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Conditional interpolation paths" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Conditional interpolation paths at three timesteps. At \(t = 0\), samples are Gaussian noise (left). At \(t = 1\), they match the target data distribution in C-space (right). The middle panel shows \(t = 0.5\) with pink lines connecting paired noise-data samples, illustrating the straight-line transport. </div> <h3 id="from-flow-matching-to-conditional-flow-matching">From Flow Matching to Conditional Flow Matching</h3> <p>If we wanted to learn the velocity field directly, we’d need to minimize the <strong>Flow Matching objective</strong>:</p> \[\mathcal{L}_{\text{FM}} = \mathbb{E}_{t,\, p_t(x_t)} \left[ \| v_\theta(x_t, t) - u_t(x_t) \|^2 \right]\] <p>where \(u_t(x_t)\) is the marginal velocity field — the aggregate velocity at position \(x_t\) and time \(t\), averaged over all possible noise-data pairings that could pass through that point. The problem is that we don’t have access to either \(p_t\) (the marginal distribution at time \(t\)) or \(u_t\) (the marginal velocity). Computing them would require integrating over all possible pairings — intractable in practice.</p> <p>The breakthrough in <d-cite key="Lipman_2023"></d-cite> is showing that we can instead optimize the <strong>Conditional Flow Matching (CFM) objective</strong>:</p> \[\mathcal{L}_{\text{CFM}} = \mathbb{E}_{t,\, q(z),\, p_t(x_t | z)} \left[ \| v_\theta(x_t, t) - u_t(x_t | z) \|^2 \right]\] <p>where \(z = (x_0, x_1)\) is a specific noise-data pair, and \(u_t (x_t \| z) = x_1 - x_0\) is the conditional velocity for that pair.</p> <p>The key result: the gradients of both objectives are identical.</p> \[\nabla_\theta \mathcal{L}_{\text{FM}}(\theta) = \nabla_\theta \mathcal{L}_{\text{CFM}}(\theta)\] <p>This means we never need to compute the intractable marginal quantities. We just sample pairs, compute straight-line velocities, and regress. The derivation uses the Fubini-Tonelli theorem to swap the order of integration and a marginalization argument — the details are in the original paper, but the practical upshot is that training reduces to a simple regression problem.</p> <h3 id="comparison-with-other-generative-approaches">Comparison with Other Generative Approaches</h3> <p>Flow matching is part of a broader family of methods that learn to transform noise into data. It helps to see where it sits relative to two other approaches:</p> <p><strong>Score Matching</strong> learns the gradient of the log-density, \(\nabla_x \log p_t(x)\). Sampling then requires running Langevin dynamics — an iterative process that mixes gradient steps with noise injections. The stochastic sampling can be slow, and the score function can be difficult to estimate accurately in low-density regions.</p> <p><strong>Denoising Diffusion (DDPM)</strong> defines a forward process that gradually corrupts data with Gaussian noise over many timesteps, then trains a network to reverse each step <d-cite key="Ho_2020"></d-cite>. Sampling involves hundreds or thousands of sequential denoising steps, each involving a forward pass through the network.</p> <p><strong>Flow Matching</strong> sidesteps both issues. Instead of learning a score and running stochastic dynamics, or learning to reverse a multi-step noising process, it directly learns a velocity field that defines a deterministic ODE from noise to data. The interpolation paths are straight lines, the training target is a simple difference \(x_1 - x_0\), and sampling requires integrating a single ODE — which can be done in as few as 10–100 Euler steps. The result is a method that is simpler to implement, faster to sample from, and often produces higher-quality results <d-cite key="Lipman_2023"></d-cite>.</p> <h3 id="the-final-training-objective">The Final Training Objective</h3> <p>Putting it all together, the training procedure samples a noise point \(x_0 \sim \mathcal{N}(0, I)\), a data point \(x_1\) from the training set, and a time \(t \sim \mathcal{U}[0, 1]\). It constructs the interpolant \(x_t = (1 - t)\, x_0 + t\, x_1\) and regresses the network output against the target velocity:</p> \[\mathcal{L} = \| v_\theta(x_t, t) - (x_1 - x_0) \|^2\] <p>That’s it. No ELBO, no adversarial loss, no score estimation. Just an MSE regression on straight-line velocities.</p> <h2 id="code-implementation">Code Implementation</h2> <p>The full implementation is in the companion notebook. Here I’ll walk through the key components.</p> <h3 id="the-model">The Model</h3> <p>The velocity field \(v_\theta(x_t, t)\) is parameterized by an MLP with sinusoidal time embeddings — the same idea as positional encodings in transformers. Time \(t\) is mapped to a high-dimensional feature vector via sinusoidal functions, projected, and added to the data projection before passing through a stack of feedforward blocks:</p> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="k">class</span> <span class="nc">FlowMLP</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">dim_data</span><span class="o">=</span><span class="mi">2</span><span class="p">,</span> <span class="n">layers</span><span class="o">=</span><span class="mi">5</span><span class="p">,</span> <span class="n">channels</span><span class="o">=</span><span class="mi">512</span><span class="p">,</span> <span class="n">dim_t</span><span class="o">=</span><span class="mi">512</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">dim_t</span> <span class="o">=</span> <span class="n">dim_t</span>
        <span class="n">self</span><span class="p">.</span><span class="n">in_proj</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">dim_data</span><span class="p">,</span> <span class="n">channels</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">t_proj</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">dim_t</span><span class="p">,</span> <span class="n">channels</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">blocks</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Sequential</span><span class="p">(</span><span class="o">*</span><span class="p">[</span><span class="nc">Block</span><span class="p">(</span><span class="n">channels</span><span class="p">)</span> <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">layers</span><span class="p">)])</span>
        <span class="n">self</span><span class="p">.</span><span class="n">out_proj</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">channels</span><span class="p">,</span> <span class="n">dim_data</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">sinusoidal_embedding</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">t</span><span class="p">,</span> <span class="n">max_positions</span><span class="o">=</span><span class="mi">10000</span><span class="p">):</span>
        <span class="n">t</span> <span class="o">=</span> <span class="n">t</span> <span class="o">*</span> <span class="n">max_positions</span>
        <span class="n">half</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">dim_t</span> <span class="o">//</span> <span class="mi">2</span>
        <span class="n">freqs</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="n">half</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="n">t</span><span class="p">.</span><span class="n">device</span><span class="p">).</span><span class="nf">float</span><span class="p">()</span>
        <span class="n">freqs</span> <span class="o">=</span> <span class="n">freqs</span><span class="p">.</span><span class="nf">mul</span><span class="p">(</span><span class="o">-</span><span class="n">math</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">max_positions</span><span class="p">)</span> <span class="o">/</span> <span class="p">(</span><span class="n">half</span> <span class="o">-</span> <span class="mi">1</span><span class="p">)).</span><span class="nf">exp</span><span class="p">()</span>
        <span class="n">args</span> <span class="o">=</span> <span class="n">t</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">*</span> <span class="n">freqs</span><span class="p">[</span><span class="bp">None</span><span class="p">,</span> <span class="p">:]</span>
        <span class="n">emb</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">args</span><span class="p">.</span><span class="nf">sin</span><span class="p">(),</span> <span class="n">args</span><span class="p">.</span><span class="nf">cos</span><span class="p">()],</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">emb</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">,</span> <span class="n">t</span><span class="p">):</span>
        <span class="n">x</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">in_proj</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        <span class="n">t</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">sinusoidal_embedding</span><span class="p">(</span><span class="n">t</span><span class="p">)</span>
        <span class="n">t</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">t_proj</span><span class="p">(</span><span class="n">t</span><span class="p">)</span>
        <span class="n">x</span> <span class="o">=</span> <span class="n">x</span> <span class="o">+</span> <span class="n">t</span>  <span class="c1"># additive conditioning
</span>        <span class="n">x</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">blocks</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">out_proj</span><span class="p">(</span><span class="n">x</span><span class="p">)</span></code></pre></figure> <p>The additive conditioning — projecting \(x\) and \(t\) separately then summing — is simple and works well for low-dimensional problems. For higher-dimensional data you’d typically use adaptive normalization (FiLM layers), but for 2D configuration space this is more than sufficient.</p> <h3 id="training">Training</h3> <p>Each training step implements the CFM objective directly:</p> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="k">for</span> <span class="n">step</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">100_000</span><span class="p">):</span>
    <span class="n">x1</span> <span class="o">=</span> <span class="n">data</span><span class="p">[</span><span class="n">torch</span><span class="p">.</span><span class="nf">randint</span><span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">data</span><span class="p">),</span> <span class="p">(</span><span class="n">batch_size</span><span class="p">,))]</span>  <span class="c1"># sample data
</span>    <span class="n">x0</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn_like</span><span class="p">(</span><span class="n">x1</span><span class="p">)</span>                           <span class="c1"># sample noise
</span>    <span class="n">t</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">rand</span><span class="p">(</span><span class="n">batch_size</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="n">device</span><span class="p">)</span>            <span class="c1"># sample time
</span>
    <span class="n">xt</span> <span class="o">=</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">t</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">])</span> <span class="o">*</span> <span class="n">x0</span> <span class="o">+</span> <span class="n">t</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">*</span> <span class="n">x1</span>        <span class="c1"># interpolate
</span>    <span class="n">target</span> <span class="o">=</span> <span class="n">x1</span> <span class="o">-</span> <span class="n">x0</span>                                    <span class="c1"># target velocity
</span>
    <span class="n">pred</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">xt</span><span class="p">,</span> <span class="n">t</span><span class="p">)</span>
    <span class="n">loss</span> <span class="o">=</span> <span class="p">((</span><span class="n">pred</span> <span class="o">-</span> <span class="n">target</span><span class="p">)</span> <span class="o">**</span> <span class="mi">2</span><span class="p">).</span><span class="nf">mean</span><span class="p">()</span>

    <span class="n">optim</span><span class="p">.</span><span class="nf">zero_grad</span><span class="p">()</span>
    <span class="n">loss</span><span class="p">.</span><span class="nf">backward</span><span class="p">()</span>
    <span class="n">optim</span><span class="p">.</span><span class="nf">step</span><span class="p">()</span></code></pre></figure> <p>Five lines of logic. Sample data, sample noise, sample time, build interpolant, regress on the velocity. The training data consists of 5,000 collision-free configurations obtained by rejection sampling — the only part of the pipeline that requires explicit collision checking.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/flow-matching-robotics/fm-training-loss-480.webp 480w,/assets/img/flow-matching-robotics/fm-training-loss-800.webp 800w,/assets/img/flow-matching-robotics/fm-training-loss-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/flow-matching-robotics/fm-training-loss.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Training loss" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Training loss (smoothed MSE) over 100k steps with AdamW at learning rate \(10^{-4}\). The loss converges quickly and stabilizes, indicating the network has learned the velocity field. </div> <h3 id="sampling">Sampling</h3> <p>At inference, we draw Gaussian noise and integrate the learned ODE forward using Euler steps:</p> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="n">xt</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">n_samples</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="n">device</span><span class="p">)</span>  <span class="c1"># start from noise
</span><span class="n">dt</span> <span class="o">=</span> <span class="mf">1.0</span> <span class="o">/</span> <span class="n">euler_steps</span>

<span class="k">for</span> <span class="n">t_val</span> <span class="ow">in</span> <span class="n">torch</span><span class="p">.</span><span class="nf">linspace</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">euler_steps</span><span class="p">):</span>
    <span class="n">vel</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">xt</span><span class="p">,</span> <span class="n">t_val</span><span class="p">.</span><span class="nf">expand</span><span class="p">(</span><span class="n">n_samples</span><span class="p">))</span>
    <span class="n">xt</span> <span class="o">=</span> <span class="n">xt</span> <span class="o">+</span> <span class="n">dt</span> <span class="o">*</span> <span class="n">vel</span>  <span class="c1"># Euler step</span></code></pre></figure> <p>No denoising schedule, no noise injection, no classifier guidance. Just a forward ODE integration — 100 Euler steps, each a single forward pass through the MLP.</p> <h2 id="results">Results</h2> <h3 id="ode-transport-from-noise-to-free-c-space">ODE Transport: From Noise to Free C-Space</h3> <p>The snapshots below show 2,000 particles being transported from Gaussian noise (\(t = 0\)) to the target distribution (\(t = 1\)). By \(t = 0.25\), the particles have already begun organizing into the channel structure of the free C-space. By \(t = 1\), they closely match the target with only a few particles landing in obstacle regions.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/flow-matching-robotics/fm-ode-snapshots-480.webp 480w,/assets/img/flow-matching-robotics/fm-ode-snapshots-800.webp 800w,/assets/img/flow-matching-robotics/fm-ode-snapshots-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/flow-matching-robotics/fm-ode-snapshots.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="ODE integration snapshots" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> ODE integration snapshots at \(t = 0.00, 0.10, 0.25, 0.50, 0.75, 1.00\). Particles (black) start as Gaussian noise and are transported into the free C-space. Red dots at \(t = 1\) indicate the few samples that land in collision regions. </div> <p>The full animation shows this transport with individual particle traces:</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/flow-matching-robotics/fm-sampling-animation-480.webp 480w,/assets/img/flow-matching-robotics/fm-sampling-animation-800.webp 800w,/assets/img/flow-matching-robotics/fm-sampling-animation-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/flow-matching-robotics/fm-sampling-animation.gif" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Sampling animation" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Animated ODE sampling: 2,000 particles transported from Gaussian noise to the collision-free C-space over 100 Euler steps. </div> <h3 id="the-learned-velocity-field">The Learned Velocity Field</h3> <p>We can visualize what the network actually learned by evaluating \(v_\theta(x, t)\) on a grid at different timesteps. This reveals the structure of the transport:</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/flow-matching-robotics/fm-velocity-field-480.webp 480w,/assets/img/flow-matching-robotics/fm-velocity-field-800.webp 800w,/assets/img/flow-matching-robotics/fm-velocity-field-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/flow-matching-robotics/fm-velocity-field.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Learned velocity field" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Learned velocity field \(v_\theta(x, t)\) at \(t = 0.00, 0.25, 0.50, 0.75\). Early timesteps show broad sweeping vectors pushing noise toward data modes. Later timesteps show finer corrections that steer particles into the narrow free-space channels. </div> <p>At \(t = 0\), the field shows large, directional vectors — the network is making coarse decisions about which mode each particle should head toward. By \(t = 0.25\), the vectors in obstacle regions have nearly vanished (particles have already left those areas), while vectors in the free channels point inward to keep particles on track. The later timesteps show increasingly local, corrective behavior.</p> <h3 id="evaluation-do-generated-configurations-actually-work">Evaluation: Do Generated Configurations Actually Work?</h3> <p>The real test: forward-kinematics the generated joint angles and check whether the resulting arm poses collide with obstacles.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/flow-matching-robotics/fm-generated-evaluation-480.webp 480w,/assets/img/flow-matching-robotics/fm-generated-evaluation-800.webp 800w,/assets/img/flow-matching-robotics/fm-generated-evaluation-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/flow-matching-robotics/fm-generated-evaluation.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Generated sample evaluation" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Left: generated samples in C-space — red dots are collision-free, green dots collide. Center: valid arm configurations rendered in the workspace. Right: the 77 colliding configurations. The model achieves a 96.2% collision-free rate. </div> <p>Out of 2,000 generated samples, 1,923 are collision-free — a <strong>96.2% success rate</strong>. The few failures cluster near the boundaries of obstacle regions in C-space, which is expected: the boundary is where the density drops sharply, and the model has the least data to learn from.</p> <h3 id="baseline-why-not-just-sample-randomly">Baseline: Why Not Just Sample Randomly?</h3> <p>If we sampled \((\theta_1, \theta_2)\) uniformly at random across \([-\pi, \pi]^2\), the collision-free rate would be about 65.6% — determined by the fraction of C-space occupied by obstacles. Flow matching improves this to 96.2%, a substantial gain that would compound in downstream applications where every invalid sample requires an expensive re-planning step.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/flow-matching-robotics/fm-baseline-comparison-480.webp 480w,/assets/img/flow-matching-robotics/fm-baseline-comparison-800.webp 800w,/assets/img/flow-matching-robotics/fm-baseline-comparison-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/flow-matching-robotics/fm-baseline-comparison.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Baseline comparison" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Collision-free rate: random uniform sampling (65.6%) versus flow matching (96.2%). </div> <h2 id="summary">Summary</h2> <p>Flow matching provides an elegant approach to generative modeling that is both theoretically clean and practical to implement. The core insight — that we can train on straight-line conditional velocities while recovering the correct marginal flow — turns a seemingly intractable transport problem into a simple regression.</p> <p>Applied to robotics, this translates into a generator that produces valid arm configurations without explicit collision checking at test time. The model learns the shape of the free configuration space from data alone, achieving 96.2% collision-free accuracy compared to 65.6% for random sampling.</p> <p>The implementation is minimal: an MLP with sinusoidal time conditioning, trained for 100k steps on an MSE loss. Sampling is a 100-step Euler integration — deterministic, fast, and parallelizable.</p> <p>For higher-dimensional configuration spaces or more complex obstacle geometries, the same framework applies — you’d just need a larger network, more training data, and potentially a more expressive architecture (U-Nets or transformers instead of an MLP). The underlying math stays the same.</p> <h2 id="glossary">Glossary</h2> <p><strong>Configuration Space (C-space):</strong> The space of all possible joint angle combinations for a robot. For a 2-link arm, C-space is \((\theta_1, \theta_2) \in [-\pi, \pi]^2\). Obstacles in the workspace carve out forbidden regions in C-space.</p> <p><strong>Flow Matching:</strong> A generative modeling framework that learns a velocity field defining an ODE from a simple source distribution (Gaussian noise) to a target data distribution. Training uses straight-line conditional interpolation paths.</p> <p><strong>Conditional Flow Matching (CFM):</strong> The tractable version of the flow matching objective. Instead of computing intractable marginal quantities, it conditions on specific noise-data pairs and regresses on their straight-line velocity \(x_1 - x_0\).</p> <p><strong>Euler Integration:</strong> A first-order numerical method for solving ODEs. Given \(\frac{dx}{dt} = v(x, t)\), the update is \(x_{t+\Delta t} = x_t + \Delta t \cdot v(x_t, t)\). Simple but effective for smooth velocity fields.</p> <p><strong>Sinusoidal Time Embedding:</strong> A technique borrowed from transformer positional encodings. Maps a scalar \(t \in [0, 1]\) to a high-dimensional vector using sine and cosine functions at different frequencies, giving the network a rich representation of time.</p>]]></content><author><name>Ricardo Huaman</name></author><category term="flow"/><category term="matching,"/><category term="generative"/><category term="models,"/><category term="robotics,"/><category term="configuration"/><category term="space,"/><category term="sampling"/><summary type="html"><![CDATA[Learning to generate collision-free robot configurations by transporting noise to data — from the math behind flow matching to a working implementation on a 2-link arm]]></summary></entry><entry><title type="html">Introduction to Sampling Algorithms</title><link href="https://rotvie.github.io/blog/2026/introduction-to-sampling-algorithms/" rel="alternate" type="text/html" title="Introduction to Sampling Algorithms"/><published>2026-03-07T00:00:00+00:00</published><updated>2026-03-07T00:00:00+00:00</updated><id>https://rotvie.github.io/blog/2026/introduction-to-sampling-algorithms</id><content type="html" xml:base="https://rotvie.github.io/blog/2026/introduction-to-sampling-algorithms/"><![CDATA[<h2 id="introduction-to-sampling-algorithms">Introduction to Sampling Algorithms</h2> <p>If you’re reading this blogpost, you’re probably accessing it through a computer of some sort. Computers are fundamentally deterministic machines. Yet many algorithms rely on randomness. In practice, computers simulate randomness using pseudorandom number generators (PRNGs) — deterministic algorithms that produce sequences of numbers that behave like random samples <d-cite key="Devroye_1986"></d-cite>.</p> <p>With this in mind, let’s assume we already have a computer that can generate samples from a uniform distribution:</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/introduction-to-sampling-algorithms/sampling-uniform-blackbox-480.webp 480w,/assets/img/introduction-to-sampling-algorithms/sampling-uniform-blackbox-800.webp 800w,/assets/img/introduction-to-sampling-algorithms/sampling-uniform-blackbox-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/introduction-to-sampling-algorithms/sampling-uniform-blackbox.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Uniform distribution primitive" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>This Probability Density Function (PDF) gives us numbers from 0 to 1 with equal probability — essentially a random number generator. This is our <strong>primitive</strong>, and the goal is to use it to generate samples from other, more complex PDFs like the Gaussian distribution.</p> <p>To be precise: the problem is to take this primitive and produce samples whose empirical distribution approximates a target PDF. This process is called <strong>sampling</strong>.</p> <h2 id="why-sampling-is-hard">Why sampling is hard?</h2> <p>Sampling from a probability density function is not as easy as it may seem. Suppose you have the following Gaussian and your task is to click along the x-axis to recreate its shape:</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/introduction-to-sampling-algorithms/sampling-why-hard-480.webp 480w,/assets/img/introduction-to-sampling-algorithms/sampling-why-hard-800.webp 800w,/assets/img/introduction-to-sampling-algorithms/sampling-why-hard-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/introduction-to-sampling-algorithms/sampling-why-hard.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Why sampling is hard" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Intuition tells us to select samples from the middle more frequently than from the tails. With some effort, we could get something that roughly resembles a Gaussian. But the real question is: <strong>how do we give this intuition to a computer?</strong></p> <h2 id="inverse-transform-sampling">Inverse Transform Sampling</h2> <p>This is the most straightforward sampling method. Let’s start by defining a few terms.</p> <h3 id="cumulative-distribution-function-cdf">Cumulative Distribution Function (CDF)</h3> <p>The cumulative distribution function \(F_X(x)\) of a real-valued random variable \(X\), evaluated at \(x\), is the probability that \(X\) will take a value less than or equal to \(x\):</p> \[F_X(x) = \int_{-\infty}^{x} f_X(t) \, dt\] <p>where \(f_X(t)\) is the PDF of \(X\), \(x\) is the upper limit of integration, and \(t\) is the variable of integration spanning from \(-\infty\) to \(x\).</p> <p>As a concrete example, consider the exponential distribution:</p> \[f_X(x) = \lambda e^{-\lambda x}, \quad x \geq 0, \quad \lambda &gt; 0\] <p>which looks like this:</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/introduction-to-sampling-algorithms/sampling-exponential-pdf-480.webp 480w,/assets/img/introduction-to-sampling-algorithms/sampling-exponential-pdf-800.webp 800w,/assets/img/introduction-to-sampling-algorithms/sampling-exponential-pdf-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/introduction-to-sampling-algorithms/sampling-exponential-pdf.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Exponential PDF" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Applying the CDF definition:</p> <p>Since the exponential PDF is zero for \(x &lt; 0\), the integral reduces to:</p> \[F_X(x) = \int_{0}^{x} \lambda e^{-\lambda t} \, dt = \left[-e^{-\lambda t}\right]_{0}^{x} = 1 - e^{-\lambda x}, \quad x \geq 0\] <p>Plotting this CDF:</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/introduction-to-sampling-algorithms/sampling-exponential-cdf-480.webp 480w,/assets/img/introduction-to-sampling-algorithms/sampling-exponential-cdf-800.webp 800w,/assets/img/introduction-to-sampling-algorithms/sampling-exponential-cdf-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/introduction-to-sampling-algorithms/sampling-exponential-cdf.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Exponential CDF" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Now think of the y-axis of the CDF curve, which ranges from 0 to 1. If you pick a random point on this y-axis, draw a horizontal line until you hit the CDF curve, and then drop a vertical line down to the x-axis — you’ve just performed inverse transform sampling.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/introduction-to-sampling-algorithms/sampling-geometric-construction-480.webp 480w,/assets/img/introduction-to-sampling-algorithms/sampling-geometric-construction-800.webp 800w,/assets/img/introduction-to-sampling-algorithms/sampling-geometric-construction-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/introduction-to-sampling-algorithms/sampling-geometric-construction.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Geometric construction of inverse transform sampling" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>The key insight: a CDF is steeper where the probability density is higher. This means a larger chunk of the y-axis maps to the more likely x-values. Picking uniformly on the y-axis naturally produces the correct clustering on the x-axis.</p> <h3 id="inverse-cumulative-distribution-function">Inverse Cumulative Distribution Function</h3> <p>With this insight, we can derive a sampling formula by inverting the CDF. Starting from the exponential CDF:</p> \[y = 1 - e^{-\lambda x}\] <p>Solving for \(x\):</p> \[\begin{align} e^{-\lambda x} &amp;= 1 - y \\ -\lambda x &amp;= \ln(1 - y) \\ x &amp;= -\frac{1}{\lambda} \ln(1 - y) \end{align}\] <p>Since \(y \sim \text{Uniform}(0, 1)\), we note that subtracting a uniform variable from 1 preserves the uniform distribution (\(1 - y \sim \text{Uniform}(0,1)\)), so we can simplify to:</p> \[F_X^{-1}(u) = -\frac{1}{\lambda} \ln(u), \quad u \sim \text{Uniform}(0, 1)\] <p>We can now plug uniform random values from our primitive into this formula and watch the exponential distribution emerge:</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/introduction-to-sampling-algorithms/sampling-its-animation-480.webp 480w,/assets/img/introduction-to-sampling-algorithms/sampling-its-animation-800.webp 800w,/assets/img/introduction-to-sampling-algorithms/sampling-its-animation-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/introduction-to-sampling-algorithms/sampling-its-animation.gif" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Inverse transform sampling animation" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <h2 id="rejection-sampling">Rejection Sampling</h2> <p>So far so good — but obtaining the inverse CDF is not always tractable. Many distributions don’t have a closed-form inverse, which makes inverse transform sampling impractical. Let’s look at a method that sidesteps this limitation entirely.</p> <p>Rejection sampling requires two ingredients: (1) a target distribution $p(x)$ that we want to sample from, and (2) a proposal distribution $q(x)$ that we already know how to sample from and that roughly captures the shape of the target <d-cite key="VonNeumann_1951"></d-cite>. We then define a scaled proposal \(cq(x)\), where \(c\) is a constant chosen such that</p> \[cq(x) \geq p(x), \quad \forall x\] <p>As a concrete example, let’s define a target \(p(x) = e^{-x^2/2} \cdot (\sin^2(6+x) + 3 \cdot \cos^2(x) \cdot \sin^2(4x) + 1)\) on the domain \([-3, 3]\). Note that this function does not need to be normalized — rejection sampling only requires evaluating the target up to a constant factor. For the proposal, we’ll use a uniform distribution on the same domain with \(c = 25\):</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/introduction-to-sampling-algorithms/sampling-proposal-envelopes-480.webp 480w,/assets/img/introduction-to-sampling-algorithms/sampling-proposal-envelopes-800.webp 800w,/assets/img/introduction-to-sampling-algorithms/sampling-proposal-envelopes-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/introduction-to-sampling-algorithms/sampling-proposal-envelopes.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Proposal and target distributions" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Now suppose we sample \(x = -1\) from the proposal. We need to decide whether to accept or reject this sample. To build intuition, let’s rotate the plot clockwise and look at the vertical line from the x-axis through the target value up to the proposal envelope:</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/introduction-to-sampling-algorithms/sampling-rotated-view-480.webp 480w,/assets/img/introduction-to-sampling-algorithms/sampling-rotated-view-800.webp 800w,/assets/img/introduction-to-sampling-algorithms/sampling-rotated-view-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/introduction-to-sampling-algorithms/sampling-rotated-view.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Rotated view of rejection sampling" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Along this line, we sample a uniform number \(u\). If \(u\) lands beyond the target value, we reject the sample; if it lands within, we accept. This is the core mechanism — points in high-density regions of \(p(x)\) are accepted more often, naturally reconstructing the target shape.</p> <p>Formalizing this:</p> <p><strong>Algorithm: Rejection sampling</strong></p> \[\begin{array}{l} \textbf{Input: } \text{target } p(x), \text{ proposal } q(x), \text{ constant } c \text{ such that } cq(x) \geq p(x) \; \forall x \\ \textbf{Output: } \text{a sample from } p(x) \\ \\ \textbf{repeat:} \\ \quad x \sim q(x) \\ \quad u \sim \text{Uniform}(0, 1) \\ \quad \textbf{if } u \leq \dfrac{p(x)}{c \cdot q(x)}: \\ \quad\quad \textbf{return } x \\ \end{array}\] <p>Notice that the choice of proposal matters. If we use a distribution that more tightly envelops the target — say, a scaled Gaussian instead of a uniform — we increase the acceptance rate and converge faster:</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/introduction-to-sampling-algorithms/sampling-rejection-animation-480.webp 480w,/assets/img/introduction-to-sampling-algorithms/sampling-rejection-animation-800.webp 800w,/assets/img/introduction-to-sampling-algorithms/sampling-rejection-animation-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/introduction-to-sampling-algorithms/sampling-rejection-animation.gif" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Rejection sampling animation" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <h2 id="importance-sampling">Importance Sampling</h2> <p>This will be our last method for this blogpost. The name is somewhat misleading — importance sampling is not really a sampling algorithm in the same sense as the previous two. It’s a <strong>variance reduction technique</strong> for estimating expectations. Still, it’s foundational to many applications and connects directly to the methods we’ve covered, so it’s worth understanding here.</p> <h3 id="expected-value">Expected Value</h3> <p>Before we get to importance sampling itself, we need to establish the tools it builds on. The first is the expected value.</p> <p>The expected value of a discrete random variable \(X\) is defined as:</p> \[E[X] = \sum_{i} x_i \cdot P(X = x_i)\] <p>For a continuous random variable with PDF \(f_X(x)\):</p> \[E[X] = \int_{-\infty}^{\infty} x \cdot f_X(x) \, dx\] <p>Intuitively, the expected value is a weighted average: each possible value of \(X\) is weighted by how likely it is to occur. Values in high-density regions of the PDF contribute more to the sum than values in the tails.</p> <p>For example, if \(X \sim \text{Uniform}(0, 1)\), the expected value is:</p> \[E[X] = \int_{0}^{1} x \cdot 1 \, dx = \frac{1}{2}\] <p>No surprises — the average of a uniform distribution over \([0, 1]\) is \(0.5\).</p> <h3 id="law-of-the-unconscious-statistician-lotus">Law of the Unconscious Statistician (LOTUS)</h3> <p>Now suppose we don’t care about \(X\) itself, but about some function of it — say \(g(X)\). What is \(E[g(X)]\)?</p> <p>The naive approach would be: figure out the distribution of the new random variable \(Y = g(X)\), then compute \(E[Y]\) using that distribution. This can be painful — deriving the PDF of a transformed variable requires change-of-variables techniques and Jacobians.</p> <p>LOTUS gives us a shortcut. It says we can compute the expected value of \(g(X)\) directly using the original PDF of \(X\):</p> \[E[g(X)] = \int_{-\infty}^{\infty} g(x) \cdot f_X(x) \, dx\] <p>No need to derive the distribution of \(g(X)\) — we just integrate \(g(x)\) weighted by the density of \(X\). The name is somewhat tongue-in-cheek: it’s the law that statisticians use “unconsciously” because it feels so natural.</p> <p>This result is what connects expected values to sampling. If we can draw samples \(x_1, x_2, \dots, x_N\) from \(f_X(x)\), then we can approximate this integral by averaging \(g\) over the samples — which is exactly what Monte Carlo methods do.</p> <h3 id="monte-carlo-methods">Monte Carlo Methods</h3> <p>Monte Carlo estimation is the idea that we can approximate an expected value by drawing samples and averaging <d-cite key="Robert_Casella_2004"></d-cite>. Given \(N\) samples \(x_1, x_2, \dots, x_N\) drawn from \(f_X(x)\), the Monte Carlo estimator is:</p> \[E[g(X)] \approx \hat{\mu}_N = \frac{1}{N} \sum_{i=1}^{N} g(x_i), \quad x_i \sim f_X(x)\] <p>Notice that \(f_X(x)\) doesn’t appear explicitly in the sum. This is because the sampling process already accounts for it — since each \(x_i\) is drawn from \(f_X\), values in high-density regions appear more frequently in the sample, and values in low-density regions appear rarely. The weighting by \(f_X(x)\) is built into the distribution of the samples themselves.</p> <p>By the <strong>Law of Large Numbers</strong>, this estimator converges to the true expected value as \(N \to \infty\). And by the <strong>Central Limit Theorem</strong>, for large \(N\) the estimator is approximately normally distributed:</p> \[\hat{\mu}_N \approx \mathcal{N}\left(E[g(X)], \; \frac{\text{Var}(g(X))}{N}\right)\] <p>The variance of the estimator shrinks as \(\frac{1}{N}\) — so to halve the error, we need four times as many samples. This is where importance sampling enters the picture.</p> <h3 id="the-importance-sampling-trick">The Importance Sampling Trick</h3> <p>Monte Carlo works great when we can sample from the target distribution \(p(x)\). But what if sampling from \(p\) is difficult, or what if naive sampling produces estimates with high variance? Importance sampling addresses both problems <d-cite key="Kahn_Marshall_1953"></d-cite>.</p> <p>The idea behind importance sampling starts with a simple algebraic trick. Take the expectation we want to compute:</p> \[E_p[g(X)] = \int g(x) \cdot p(x) \, dx\] <p>Now multiply and divide by a proposal distribution $q(x)$ that we <em>can</em> sample from, provided that $q(x) &gt; 0$ wherever $p(x) \neq 0$ (the proposal must have support everywhere the target does):</p> \[E_p[g(X)] = \int g(x) \cdot \frac{p(x)}{q(x)} \cdot q(x) \, dx\] <p>Look at what happened — this is now an expectation under \(q\):</p> \[E_p[g(X)] = E_q\left[g(X) \cdot \frac{p(X)}{q(X)}\right]\] <p>We’ve rewritten an expectation under a distribution we can’t sample from (\(p\)) as an expectation under one we can (\(q\)). The cost is that we need to correct each sample with the ratio \(\frac{p(x)}{q(x)}\), called the <strong>importance weight</strong>.</p> <p>The Monte Carlo estimator becomes:</p> \[E_p[g(X)] \approx \frac{1}{N} \sum_{i=1}^{N} g(x_i) \cdot \frac{p(x_i)}{q(x_i)}, \quad x_i \sim q(x)\] <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/introduction-to-sampling-algorithms/sampling-is-trick-480.webp 480w,/assets/img/introduction-to-sampling-algorithms/sampling-is-trick-800.webp 800w,/assets/img/introduction-to-sampling-algorithms/sampling-is-trick-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/introduction-to-sampling-algorithms/sampling-is-trick.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Importance sampling trick" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <h3 id="building-intuition-a-rigged-die">Building Intuition: A Rigged Die</h3> <p>To see why this works, consider a concrete example. Imagine a rigged die where face 6 comes up half the time, while faces 1 through 5 each appear with probability \(\frac{1}{10}\). We want to compute the expected value under this rigged die, but all we have available is a fair die.</p> <p>If we roll the fair die \(N\) times, each face shows up roughly \(\frac{1}{6}\) of the time. But under the rigged distribution, face 6 should appear in \(\frac{1}{2}\) of our rolls — the frequencies are wrong.</p> <p>The importance weight corrects this mismatch. For face 6, the weight is \(\frac{P_{\text{rigged}}(6)}{P_{\text{fair}}(6)} = \frac{1/2}{1/6} = 3\) — each observation of a 6 contributes its value multiplied by a weight of 3. For face 1, the weight is \(\frac{1/10}{1/6} = 0.6\) — we downweight it because the fair die overrepresents it relative to the rigged distribution.</p> <p>The importance weights compensate for the mismatch between the distribution we’re sampling from and the distribution we actually care about.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/introduction-to-sampling-algorithms/sampling-rigged-die-480.webp 480w,/assets/img/introduction-to-sampling-algorithms/sampling-rigged-die-800.webp 800w,/assets/img/introduction-to-sampling-algorithms/sampling-rigged-die-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/introduction-to-sampling-algorithms/sampling-rigged-die.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Rigged die importance weights" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <h3 id="why-not-just-use-any-q">Why Not Just Use Any \(q\)?</h3> <p>Mathematically, any \(q(x) &gt; 0\) wherever \(p(x) &gt; 0\) will give a correct estimator. But the choice of \(q\) dramatically affects the <strong>variance</strong>.</p> <p>If \(q\) is very different from \(p\), some samples will have enormous importance weights while most will have tiny ones. A few samples dominate the sum, and the estimate becomes noisy. In the extreme, you might need millions of samples to get a reasonable approximation.</p> <p>The optimal proposal is:</p> \[q^*(x) \propto |g(x)| \cdot p(x)\] <p>This proposal minimizes the variance of the importance sampling estimator. It concentrates samples where the integrand \(g(x) \cdot p(x)\) is large — the “important” regions. This is where the name comes from: we sample more from the parts of the space that matter most to the integral.</p> <p>In practice, we can’t usually compute \(q^*\) exactly (if we could, we wouldn’t need Monte Carlo in the first place). But even a rough approximation to it can reduce variance by orders of magnitude compared to naive sampling — and that’s what makes importance sampling so powerful.</p> <p>To see this in action, suppose we want to estimate \(E_p[X^2]\) where \(p(x) = \mathcal{N}(2, 0.5^2)\). We compare two proposals: a good one centered near the target (\(q_{good} = \mathcal{N}(2, 0.8^2)\)) and a bad one far away (\(q_{bad} = \mathcal{N}(-1, 2^2)\)). Watch how the good proposal converges smoothly to the true value, while the bad proposal is erratic — dominated by rare samples with extreme importance weights:</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/introduction-to-sampling-algorithms/sampling-convergence-480.webp 480w,/assets/img/introduction-to-sampling-algorithms/sampling-convergence-800.webp 800w,/assets/img/introduction-to-sampling-algorithms/sampling-convergence-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/introduction-to-sampling-algorithms/sampling-convergence.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Good vs bad proposal convergence" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/introduction-to-sampling-algorithms/sampling-is-animation-480.webp 480w,/assets/img/introduction-to-sampling-algorithms/sampling-is-animation-800.webp 800w,/assets/img/introduction-to-sampling-algorithms/sampling-is-animation-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/introduction-to-sampling-algorithms/sampling-is-animation.gif" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Importance sampling animation" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <h2 id="summary">Summary</h2> <p>We started with a single primitive — a uniform random number generator — and built three increasingly flexible methods to sample from arbitrary distributions. Each method makes a different tradeoff:</p> <table> <thead> <tr> <th>Method</th> <th>What it needs</th> <th>Strength</th> <th>Mode of failure</th> </tr> </thead> <tbody> <tr> <td>Inverse Transform Sampling</td> <td>Closed-form $F_X^{-1}$</td> <td>Exact samples, no waste</td> <td>Most distributions don’t have an invertible CDF</td> </tr> <tr> <td>Rejection Sampling</td> <td>A proposal $cq(x) \geq p(x)$</td> <td>Works for any evaluable target</td> <td>Poor proposal choice leads to massive rejection rates — most samples are thrown away</td> </tr> <tr> <td>Importance Sampling</td> <td>A proposal $q(x)$ with overlapping support</td> <td>Doesn’t discard samples; reweights instead</td> <td>Bad $q$ causes a few samples to dominate via extreme weights, making the estimate unreliable</td> </tr> </tbody> </table> <p>A pattern emerges across all three: <strong>the quality of your results depends on how well you understand the shape of the distribution you’re targeting.</strong> Inverse transform sampling requires you to know the CDF so well that you can invert it analytically. Rejection sampling requires a proposal that tightly envelops the target — too loose and you waste most of your computation on rejected points. Importance sampling requires a proposal that places mass where the integrand matters — too far off and a handful of samples with extreme weights will dominate your estimate.</p> <p>This is not the end of the story. All three methods share a fundamental limitation: they struggle in high-dimensional spaces. As the number of dimensions grows, the volume of space increases exponentially, and most of the probability mass concentrates in thin regions of the space. The probability of generating a useful sample drops just as fast. Rejection sampling acceptance rates collapse, and importance weights become increasingly degenerate.</p> <p>This is where <strong>Markov Chain Monte Carlo (MCMC)</strong> methods come in <d-cite key="Robert_Casella_2004"></d-cite>. Instead of generating independent samples and hoping they land in the right place, MCMC constructs a Markov chain whose stationary distribution is the target distribution, generating correlated samples that gradually explore the space by taking small, informed steps. But that’s a topic for another post.</p>]]></content><author><name>Ricardo Huaman</name></author><category term="sampling,"/><category term="monte"/><category term="carlo,"/><category term="statistics,"/><category term="probability"/><summary type="html"><![CDATA[From a uniform random number generator to inverse transform sampling, rejection sampling, and importance sampling — building intuition for how computers draw samples from arbitrary distributions]]></summary></entry><entry><title type="html">The Multi-Armed Bandit Problem</title><link href="https://rotvie.github.io/blog/2026/multi-armed-bandits/" rel="alternate" type="text/html" title="The Multi-Armed Bandit Problem"/><published>2026-02-23T00:00:00+00:00</published><updated>2026-02-23T00:00:00+00:00</updated><id>https://rotvie.github.io/blog/2026/multi-armed-bandits</id><content type="html" xml:base="https://rotvie.github.io/blog/2026/multi-armed-bandits/"><![CDATA[<h2 id="the-multi-armed-bandit-problem">The Multi-Armed Bandit Problem</h2> <p>There are many resources that explain multi-armed bandits as “a smarter A/B test.” While that’s one application, it undersells what’s going on. The bandit problem captures something more fundamental: <strong>how do you make good decisions when every choice teaches you something about the world?</strong> <d-cite key="Slivkins_2019"></d-cite></p> <p>This post builds the full picture — starting from a problem every content creator faces, formalizing it into math, and then walking through three algorithms that solve it in fundamentally different ways.</p> <h2 id="you-just-uploaded-a-video">You Just Uploaded a Video</h2> <p>You’ve spent a week editing a video and it’s ready to publish. But which thumbnail should you use? You’ve prepared <strong>5 candidates</strong> — different compositions, text overlays, color palettes. Each thumbnail will attract a different fraction of viewers who see it in their feed. You want to find the best one as quickly as possible.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/multi-armed-bandits/mab-five-thumbnails-480.webp 480w,/assets/img/multi-armed-bandits/mab-five-thumbnails-800.webp 800w,/assets/img/multi-armed-bandits/mab-five-thumbnails-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/multi-armed-bandits/mab-five-thumbnails.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Five thumbnail candidates" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Five thumbnail candidates for the same video. Each has an unknown click-through rate (CTR) — the fraction of viewers who click when they see it in their feed. </div> <p>YouTube actually does something like this internally. When you upload a video, it can show different thumbnails to different viewers and track which one gets the most clicks. But how should it allocate impressions across the candidates?</p> <h3 id="the-naive-approach-equal-split">The naive approach: equal split</h3> <p>The standard approach is a fixed A/B test: split impressions equally across all 5 thumbnails for a few days, then keep the winner. Simple, statistically valid — but during that test period, you’re sending <strong>equal impressions to every thumbnail, including the bad ones</strong>.</p> <p>Say T3 has an 8% CTR and T1 has a 2% CTR. An equal-split test keeps showing T1 to 20% of your audience for the entire test. Every impression spent on T1 instead of T3 is a potential click lost.</p> <h3 id="the-smarter-approach-adapt-as-you-learn">The smarter approach: adapt as you learn</h3> <p>A bandit algorithm starts by exploring all thumbnails, but as it gathers evidence, it <strong>shifts impressions toward the winners</strong> while still occasionally checking the others. Fewer wasted impressions, faster convergence to the best thumbnail.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/multi-armed-bandits/mab-ab-vs-bandit-allocation-480.webp 480w,/assets/img/multi-armed-bandits/mab-ab-vs-bandit-allocation-800.webp 800w,/assets/img/multi-armed-bandits/mab-ab-vs-bandit-allocation-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/multi-armed-bandits/mab-ab-vs-bandit-allocation.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="A/B test vs bandit allocation" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> An A/B test splits impressions equally (left). A bandit algorithm shifts traffic toward the best-performing thumbnail as evidence accumulates (right). </div> <p>This is the multi-armed bandit problem: balancing <strong>exploiting</strong> what’s working with <strong>exploring</strong> what might work better.</p> <h2 id="formal-definition">Formal Definition</h2> <p>Let’s translate this into math.</p> <p>You have $K$ thumbnail candidates. Each thumbnail $i$ has a true CTR $\theta_i$ that you don’t know. Every time an impression is served (time step $t$), you choose which thumbnail to show (action $a_t$). The viewer either clicks ($r_t = 1$) or scrolls past ($r_t = 0$):</p> \[P(r_t = 1) = \theta_{a_t}\] <p>This is called a <strong>Bernoulli bandit</strong> — each action gives a binary outcome, like a coin flip with unknown bias <d-cite key="Lattimore_Szepesvari_2020"></d-cite>.</p> <h3 id="why-multi-armed-bandit">Why “Multi-Armed Bandit”?</h3> <p>The name comes from slot machines. A “one-armed bandit” is a single slot machine — one lever, steals your money. A “multi-armed bandit” is the problem of facing $K$ machines at once, each with a different hidden payout. Pull an arm, observe the reward, decide which arm to pull next. Your 5 thumbnails are your 5 arms.</p> <h3 id="regret">Regret</h3> <p>We measure performance by <strong>regret</strong> — the total clicks lost by not always showing the best thumbnail:</p> \[\mathcal{L}_T = \sum_{t=1}^{T} (\theta^* - \theta_{a_t}), \quad \theta^* = \max_i \theta_i\] <p>Every impression spent on a suboptimal thumbnail adds some regret. A good algorithm minimizes this.</p> <h3 id="the-estimation-problem">The Estimation Problem</h3> <p>You estimate each thumbnail’s CTR from observed data:</p> \[\hat{Q}_t(a) = \frac{\text{clicks on thumbnail } a}{\text{impressions of thumbnail } a }\] <p>The catch: <strong>early estimates are noisy</strong>. After showing T3 to just 50 viewers, your estimated CTR could be anywhere from 0% to 16%, even if the true CTR is 8%. This noise is what makes the problem hard — and it’s why the three algorithms below take fundamentally different approaches to dealing with it.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/multi-armed-bandits/mab-estimation-noise-480.webp 480w,/assets/img/multi-armed-bandits/mab-estimation-noise-800.webp 800w,/assets/img/multi-armed-bandits/mab-estimation-noise-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/multi-armed-bandits/mab-estimation-noise.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Noisy estimates" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> After 50 impressions per thumbnail, error bars are wide and overlap — you can't confidently pick the winner. After 2,000 each, estimates are tight — but that's 10,000 impressions, most wasted on bad thumbnails. </div> <h2 id="algorithm-1-varepsilon-greedy">Algorithm 1: $\varepsilon$-Greedy</h2> <p>The simplest bandit strategy. Most of the time, show the thumbnail with the highest estimated CTR. Occasionally, try a random one:</p> \[a_t = \begin{cases} \displaystyle\operatorname*{argmax}_a \hat{Q}_t(a) &amp; \text{with probability } 1 - \varepsilon \quad (\text{show the current best}) \\ \text{random thumbnail} &amp; \text{with probability } \varepsilon \quad (\text{try something random}) \end{cases}\] <p>Set $\varepsilon = 0.1$ and 10% of impressions go to a random thumbnail.</p> <p><strong>Why it works (sort of):</strong> the random explorations help you discover if a different thumbnail is actually better. Over time, estimates converge and you mostly exploit the true winner.</p> <p><strong>Why it’s limited:</strong> that random 10% is <em>blind</em>. It doesn’t care whether you’ve already confirmed a thumbnail is terrible. After 10,000 impressions, you might have rock-solid evidence that T1 has a 2% CTR, and $\varepsilon$-Greedy will still randomly show it. Those are wasted impressions — viewers who could have been shown T3.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/multi-armed-bandits/mab-epsilon-greedy-results-480.webp 480w,/assets/img/multi-armed-bandits/mab-epsilon-greedy-results-800.webp 800w,/assets/img/multi-armed-bandits/mab-epsilon-greedy-results-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/multi-armed-bandits/mab-epsilon-greedy-results.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Epsilon-greedy results" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> $\varepsilon$-Greedy with different exploration rates. Too little exploration ($\varepsilon = 0$) risks getting stuck on a suboptimal thumbnail. Too much ($\varepsilon = 0.3$) wastes impressions on thumbnails already known to be bad. The regret keeps growing linearly — the waste never stops. </div> <h2 id="algorithm-2-upper-confidence-bounds-ucb1">Algorithm 2: Upper Confidence Bounds (UCB1)</h2> <h3 id="why-not-explore-smarter">Why Not Explore Smarter?</h3> <p>The problem with $\varepsilon$-Greedy is that it explores <em>blindly</em>. What if, instead of exploring randomly, we sent impressions specifically to <strong>thumbnails we’re still uncertain about?</strong></p> <h3 id="building-the-intuition">Building the Intuition</h3> <p>Imagine you’ve been running the test and here’s what you know:</p> <table> <thead> <tr> <th>Thumbnail</th> <th>Impressions</th> <th>Clicks</th> <th>Observed CTR</th> <th>How confident are you?</th> </tr> </thead> <tbody> <tr> <td>T1</td> <td>500</td> <td>10</td> <td>2.0%</td> <td>Very confident — it’s bad</td> </tr> <tr> <td>T2</td> <td>500</td> <td>26</td> <td>5.2%</td> <td>Pretty confident</td> </tr> <tr> <td>T3</td> <td>12</td> <td>1</td> <td>8.3%</td> <td><strong>Not confident at all!</strong></td> </tr> <tr> <td>T4</td> <td>500</td> <td>14</td> <td>2.8%</td> <td>Very confident — it’s bad</td> </tr> <tr> <td>T5</td> <td>500</td> <td>21</td> <td>4.2%</td> <td>Pretty confident</td> </tr> </tbody> </table> <p>T3 <em>looks</em> great at 8.3%, but you’ve only shown it 12 times. That could easily be noise — maybe nobody happened to be in the mood to click. Or maybe it really is the best. <strong>You should show it more to find out.</strong></p> <p>Meanwhile, T1 and T4 have been shown 500 times each. You’re very confident they’re bad. There’s no point sending more impressions their way.</p> <p>UCB1 formalizes exactly this intuition. For each thumbnail, it computes a score that combines two things:</p> \[\text{UCB score}(a) = \underbrace{\hat{Q}_t(a)}_{\text{estimated CTR}} + \underbrace{\sqrt{\frac{2 \ln t}{N_t(a)}}}_{\text{how uncertain you are}}\] <p>Then it shows the thumbnail with the highest score.</p> <p>The second term captures your uncertainty about the estimate. It’s large when $N_t(a)$ is small (few impressions → uncertain → explore), and shrinks as you gather data (many impressions → confident → trust the estimate).</p> <h3 id="where-does-the-uncertainty-term-come-from">Where Does the Uncertainty Term Come From?</h3> <p>It comes from <strong>Hoeffding’s Inequality</strong>, a result from probability theory that bounds how far a sample mean can deviate from the true mean <d-cite key="Auer_2002"></d-cite>.</p> <p>Hoeffding tells you: given $N$ observations of a random variable bounded in $[0,1]$, the probability that the true mean exceeds the sample mean by more than $u$ is at most $e^{-2Nu^2}$.</p> <p>If we want to be very sure (probability $\leq t^{-4}$, which is vanishingly small) that our upper bound covers the true CTR, we set $e^{-2Nu^2} = t^{-4}$ and solve:</p> \[e^{-2Nu^2} = t^{-4} \implies u = \sqrt{\frac{2\ln t}{N}}\] <p>That’s the uncertainty term. It’s a statistically rigorous confidence bound — hence the name “Upper Confidence Bound.”</p> <p>The philosophy is sometimes called <strong>“optimism in the face of uncertainty”</strong>: act as if each thumbnail is as good as it <em>plausibly could be</em> given the data. Under-explored thumbnails get the benefit of the doubt.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/multi-armed-bandits/mab-ucb-scores-480.webp 480w,/assets/img/multi-armed-bandits/mab-ucb-scores-800.webp 800w,/assets/img/multi-armed-bandits/mab-ucb-scores-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/multi-armed-bandits/mab-ucb-scores.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="UCB score decomposition" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> UCB1 scores for each thumbnail. The uncertainty term (lighter bar) is large for T3 (only 12 impressions) and tiny for well-explored thumbnails. UCB1 sends the next impression to T3 — the thumbnail it knows the least about. </div> <h2 id="algorithm-3-thompson-sampling">Algorithm 3: Thompson Sampling</h2> <h3 id="a-different-way-to-handle-uncertainty">A Different Way to Handle Uncertainty</h3> <p>UCB1 uses a formula to put a number on uncertainty. Thompson Sampling takes a different route: it maintains a <strong>full probability distribution</strong> representing your belief about each thumbnail’s true CTR, and uses randomness to decide what to explore <d-cite key="Chapelle_Li_2011"></d-cite>.</p> <h3 id="beliefs-as-beta-distributions">Beliefs as Beta Distributions</h3> <p>For each thumbnail, you keep a belief: <em>“given everything I’ve observed, what could this thumbnail’s true CTR be?”</em> This belief is a <strong>Beta distribution</strong> $\text{Beta}(\alpha, \beta)$, where:</p> <ul> <li>$\alpha$ = clicks + 1</li> <li>$\beta$ = non-clicks + 1</li> </ul> <p>If T2 has received 100 impressions, 5 clicks, 95 skips: the belief is $\text{Beta}(6, 96)$ — peaked near 5%, fairly narrow. You’re reasonably confident.</p> <p>If T3 has received 3 impressions, 1 click: the belief is $\text{Beta}(2, 3)$ — a very wide distribution spanning nearly the whole range from 0% to 70%. You have almost no idea what T3’s true CTR is.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/multi-armed-bandits/mab-beta-distributions-480.webp 480w,/assets/img/multi-armed-bandits/mab-beta-distributions-800.webp 800w,/assets/img/multi-armed-bandits/mab-beta-distributions-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/multi-armed-bandits/mab-beta-distributions.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Beta distributions at different stages" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> The Beta distribution sharpens with data. With few impressions, the belief is wide — the true CTR could be almost anything. With many impressions, the belief narrows to a tight peak near the true value. </div> <h3 id="the-algorithm">The Algorithm</h3> <p>Each time a new viewer arrives:</p> <ol> <li>For each thumbnail, <strong>draw a random value</strong> from its belief distribution.</li> <li>Show the viewer <strong>whichever thumbnail drew the highest value</strong>.</li> <li>Observe click or skip. <strong>Update the belief</strong>: click → $\alpha + 1$, skip → $\beta + 1$.</li> </ol> <p><strong>Why this works:</strong> Thumbnails you’re uncertain about have wide beliefs, so their draws occasionally land very high — they get impressions. Thumbnails you’re confident about have narrow beliefs — draws cluster near the true CTR. If it’s genuinely the best, it consistently “wins the draw.” Over time, all beliefs narrow and the best thumbnail dominates.</p> <p>This property is called <strong>probability matching</strong>: each thumbnail is selected with probability proportional to the probability that it <em>is</em> the best, given what you’ve observed <d-cite key="Russo_2017"></d-cite>.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/multi-armed-bandits/mab-thompson-one-round-480.webp 480w,/assets/img/multi-armed-bandits/mab-thompson-one-round-800.webp 800w,/assets/img/multi-armed-bandits/mab-thompson-one-round-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/multi-armed-bandits/mab-thompson-one-round.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="One round of Thompson Sampling" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> One round of Thompson Sampling. Each thumbnail's belief produces a random draw. T3, with its wide belief (only 12 impressions), draws a high value and gets explored — not because of a formula, but because uncertainty naturally produces high samples. </div> <h3 id="beliefs-converge-over-time">Beliefs Converge Over Time</h3> <p>As the algorithm runs, beliefs sharpen. Early on, all distributions are wide and the algorithm explores broadly. Eventually, the best thumbnail’s distribution dominates, and almost all impressions flow there.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/multi-armed-bandits/mab-thompson-convergence-480.webp 480w,/assets/img/multi-armed-bandits/mab-thompson-convergence-800.webp 800w,/assets/img/multi-armed-bandits/mab-thompson-convergence-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/multi-armed-bandits/mab-thompson-convergence.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Posterior convergence" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Thompson Sampling posteriors at four time steps. At $t = 0$, all beliefs are flat — "any CTR is equally plausible." By convergence, the best thumbnail (T3, 8% true CTR) has a sharp, dominant peak and receives nearly all impressions. </div> <h2 id="head-to-head-comparison">Head-to-Head Comparison</h2> <p>Now the main event. All three algorithms on the same 5-thumbnail problem (true CTRs: 2%, 3%, 4%, 5%, 8%), over 10,000 impressions, averaged across 30 runs.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/multi-armed-bandits/mab-comparison-480.webp 480w,/assets/img/multi-armed-bandits/mab-comparison-800.webp 800w,/assets/img/multi-armed-bandits/mab-comparison-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/multi-armed-bandits/mab-comparison.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Algorithm comparison" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> (a) Cumulative regret: Thompson and UCB1 flatten, while $\varepsilon$-Greedy grows linearly. (b) Log-log plot reveals the growth rate: $O(t)$ for $\varepsilon$-Greedy, $O(\log t)$ for the others. (c) Impression distribution: Thompson concentrates nearly all impressions on the best thumbnail. </div> <p>The key result: $\varepsilon$-Greedy regret grows <strong>linearly</strong> — it never stops wasting impressions. UCB1 and Thompson Sampling grow <strong>logarithmically</strong> — the regret curve flattens as they lock onto the best thumbnail. Thompson typically achieves the lowest total regret.</p> <h2 id="beyond-thumbnails">Beyond Thumbnails</h2> <p>The explore-exploit tradeoff isn’t unique to thumbnails. It appears wherever decisions must be made under uncertainty with limited feedback:</p> <ul> <li><strong>Ad placement</strong>: Which banner to show on a webpage. This is where bandits are most widely deployed in industry.</li> <li><strong>Clinical trials</strong>: Choosing between a proven treatment and a promising experimental one. Bandit algorithms have been proposed for adaptive designs that shift patients toward the more effective treatment as evidence accumulates <d-cite key="Villar_2015"></d-cite>.</li> <li><strong>Recommendation systems</strong>: Should a platform show you content it’s confident you’ll enjoy, or something new that <em>might</em> be a hit?</li> </ul> <p>Even training deep learning models involves this tradeoff. Stochastic Gradient Descent uses mini-batches that introduce noise into gradient estimates — acting as implicit exploration that helps escape local minima. It’s the same “productive randomness” that makes Thompson Sampling work.</p> <h2 id="summary">Summary</h2> <table> <thead> <tr> <th>Algorithm</th> <th>How it explores</th> <th>Regret</th> <th>Key property</th> </tr> </thead> <tbody> <tr> <td>$\varepsilon$-Greedy</td> <td>Randomly — wastes impressions on known-bad thumbnails</td> <td>Linear — $O(T)$</td> <td>Simple but inefficient</td> </tr> <tr> <td>UCB1</td> <td>Targets uncertain thumbnails via a Hoeffding-derived confidence bound</td> <td>Logarithmic — $O(\log T)$</td> <td>Deterministic, theoretically grounded</td> </tr> <tr> <td>Thompson Sampling</td> <td>Draws from belief distributions; uncertain thumbnails can “win” by chance</td> <td>Logarithmic — $O(\log T)$</td> <td>Automatic, typically lowest regret</td> </tr> </tbody> </table> <p>The core insight: <strong>information has value</strong>. Showing a viewer an under-explored thumbnail isn’t wasting that impression — it’s investing in learning which thumbnail is best, which pays off by routing all future viewers better. The question is how to make those investments wisely. $\varepsilon$-Greedy invests randomly. UCB1 and Thompson Sampling invest where it matters.</p> <h2 id="glossary">Glossary</h2> <p><strong>Click-Through Rate (CTR):</strong> The fraction of impressions that result in a click. If 1,000 viewers see a thumbnail and 50 click, the CTR is 5%.</p> <p><strong>Bernoulli Bandit:</strong> A multi-armed bandit where each arm gives a binary reward (0 or 1) with some unknown probability $\theta$.</p> <p><strong>Regret:</strong> The cumulative difference between the reward of the best arm and the arm actually chosen, summed over all time steps. Lower is better.</p> <p><strong>Upper Confidence Bound (UCB):</strong> A score combining the estimated reward with an uncertainty term derived from Hoeffding’s Inequality. Arms with fewer observations get a larger uncertainty term, encouraging exploration.</p> <p><strong>Beta Distribution:</strong> A probability distribution on the interval $[0, 1]$, parameterized by $\alpha$ and $\beta$. In Thompson Sampling, it represents the posterior belief about an arm’s true reward probability.</p> <p><strong>Probability Matching:</strong> The property that each arm is selected with probability equal to the probability it is optimal, given the observed data.</p>]]></content><author><name>Ricardo Huaman</name></author><category term="multi-armed"/><category term="bandits,"/><category term="reinforcement"/><category term="learning,"/><category term="exploration-exploitation,"/><category term="bayesian"/><category term="methods"/><summary type="html"><![CDATA[A visual walkthrough of exploration vs exploitation — from YouTube thumbnails to the algorithms behind smart experimentation]]></summary></entry><entry><title type="html">Langevin Sampling for Diffusion Models</title><link href="https://rotvie.github.io/blog/2026/langevin-sampling-for-diffusion/" rel="alternate" type="text/html" title="Langevin Sampling for Diffusion Models"/><published>2026-02-08T00:00:00+00:00</published><updated>2026-02-08T00:00:00+00:00</updated><id>https://rotvie.github.io/blog/2026/langevin-sampling-for-diffusion</id><content type="html" xml:base="https://rotvie.github.io/blog/2026/langevin-sampling-for-diffusion/"><![CDATA[<h2 id="langevin-sampling-for-diffusion-models">Langevin Sampling for Diffusion Models</h2> <p>There are many resources on the internet that explain diffusion models as a noising and denoising process using a deep learning architecture. Although those are useful, most of them do not paint the whole picture. There’s another component that’s just as important as the deep learning part: the probabilistic framework that makes it all work <d-cite key="DepthFirst_2025"></d-cite>.</p> <h2 id="assigning-probability-values-to-images">Assigning Probability Values to Images</h2> <p>Imagine a black box called the <em>image distribution</em>. This box gives us samples from an underlying distribution — that is, images of all kinds.</p> <p>These images have intrinsic likelihoods, similar to the rolling dice case. An image of a koala and an image of a tiger could have similar probabilities, while a noisy image would have much less likelihood:</p> \[p(\text{Koala}) \approx p(\text{Tiger}) &gt; p(\text{Noise})\] <p>With this in mind, all we need to do is generate samples from this image distribution, right? But first, we need to understand how to generate samples from <em>any</em> distribution using a computer. Let’s start with a simpler case: how do we generate samples from a dice-rolling distribution?</p> <h2 id="challenges-in-sampling-from-probability-distributions">Challenges in Sampling from Probability Distributions</h2> <p>Generating samples from a fair die is the simplest case. There are many libraries already implemented in Python that allow you to do this, such as <code class="language-plaintext highlighter-rouge">random</code>. But how would you implement it from first principles?</p> <p>At first you might think that if we know the PMF of the probability distribution, then we’ll be able to generate samples from it. However, this is not exactly the case — we know how the PMF looks, but actually generating samples from it requires algorithms like the Mersenne Twister or Xorshift random number generators. This sounds kind of intimidating, so let’s look at an alternative approach.</p> <p>Say we have a continuous uniform distribution and we can sample from it. Then:</p> <ol> <li>Sample from the continuous uniform distribution:</li> </ol> \[x \sim \mathcal{U}(0, 1)\] <ol> <li>Threshold on multiples of $1/6$, which gives us the dice roll values:</li> </ol> \[\begin{align*} 0 &lt; x &lt; \frac{1}{6} &amp;: \bullet \\ \frac{1}{6} &lt; x &lt; \frac{2}{6} &amp;: \bullet\bullet \\ &amp;\vdots \\ \frac{5}{6} &lt; x &lt; 1 &amp;: \bullet\bullet\bullet\bullet\bullet\bullet \end{align*}\] <p>Interesting — although both of these are uniform distributions, so the trick feels a bit circular. However, there’s another distribution that, if we could sample from it, would allow us to sample from <em>almost any</em> other distribution.</p> <h2 id="the-probability-density-function-with-two-peaks">The Probability Density Function with Two Peaks</h2> <p>Let’s assume we have a PDF called $p_{\text{twopeaks}}(x)$ — a distribution with two distinct peaks.</p> <div class="row justify-content-sm-center"> <div class="col-sm-8 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/langevin-sampling-for-diffusion/ptwopeaks-pdf-480.webp 480w,/assets/img/langevin-sampling-for-diffusion/ptwopeaks-pdf-800.webp 800w,/assets/img/langevin-sampling-for-diffusion/ptwopeaks-pdf-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/langevin-sampling-for-diffusion/ptwopeaks-pdf.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Two Peaks PDF" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> The probability density function \( p_{\text{twopeaks}}(x) \) with two distinct peaks. </div> <p>Also assume we have a function $F_{\text{twopeaks}}(x)$ which, given any input $x_i$, tells us whether we should move left or right to most quickly increase the value of $p_{\text{twopeaks}}(x)$.</p> <p>Our third assumption is that we can generate samples from a normal distribution.</p> <p>With all three assumptions in place, we can begin.</p> <h2 id="langevin-sampling-an-algorithm-that-generates-samples-from-any-probability-distribution">Langevin Sampling: An Algorithm That Generates Samples from Any Probability Distribution</h2> <p><strong>Algorithm: Langevin sampling</strong></p> \[\begin{array}{l} x_0 \sim \mathcal{N}(0, 1) \\ \textbf{for } t = 0 \text{ to } 1000: \\ \quad z_t \sim \mathcal{N}(0, 1) \\ \quad x_{t+1} = x_t + \epsilon F(x_t) + \sqrt{2\epsilon} z_t \\ \textbf{return } x_{1000} \end{array}\] <div class="row justify-content-sm-center"> <div class="col-sm-10 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/langevin-sampling-for-diffusion/langevin-sampling-480.webp 480w,/assets/img/langevin-sampling-for-diffusion/langevin-sampling-800.webp 800w,/assets/img/langevin-sampling-for-diffusion/langevin-sampling-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/langevin-sampling-for-diffusion/langevin-sampling.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Langevin Sampling Algorithm" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Langevin Sampling algorithm: iteratively applying gradient-based updates with Gaussian noise to generate samples from a target distribution. </div> <p>The Langevin Sampling algorithm is described in the image above. One iteration in the loop is called a <em>Langevin Update</em>. We iterate thousands of times (say 1,000) to generate a single sample. Then we repeat this entire process many times to build up a histogram that approximates the target distribution.</p> <p>It’s important that we add Gaussian noise at each iteration to prevent the sample from collapsing onto the peak itself. The randomness is essential — it keeps the deterministic gradient ascent from getting stuck.</p> <h2 id="intuition-for-the-score-function">Intuition for the Score Function</h2> <p>Now, let’s look at the $F$ term — the <strong>score function</strong>. It’s defined as:</p> \[F(x_t) = \nabla_x \log p(x_t)\] <p>It’s intuitive that this gradient (derivative) gives us the direction of quickest increase. But why the $\log$?</p> <p>The logarithm amplifies the magnitude of the gradient in low-density regions, allowing us to reach the most probable samples faster. Without it, the gradients in the tails of the distribution would be vanishingly small, making it hard to navigate toward the peaks.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/langevin-sampling-for-diffusion/langevin-sampling-gradient-log-480.webp 480w,/assets/img/langevin-sampling-for-diffusion/langevin-sampling-gradient-log-800.webp 800w,/assets/img/langevin-sampling-for-diffusion/langevin-sampling-gradient-log-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/langevin-sampling-for-diffusion/langevin-sampling-gradient-log.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Score Function with Log" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Effect of the logarithm on the score function: amplifying gradients in low-density regions to improve navigation toward the peaks. </div> <p>Now that we have that in place, let’s do an exercise: applying Langevin Sampling to generate samples from our dice-rolling setup. We’ll follow the strategy shown previously — first use Langevin Sampling to sample from a continuous uniform distribution (assuming we’re given a way to sample from a Gaussian distribution to perform the Langevin updates).</p> <h2 id="exercise-a-dice-roll-sampler-from-scratch-using-langevin-sampling">Exercise: A Dice Roll Sampler from Scratch Using Langevin Sampling</h2> <p>First, we need to obtain $F(x)$ for the uniform distribution in the correct sample space (from 0 to 1). If we take only the derivative, it would be zero everywhere inside the interval — not very helpful.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/langevin-sampling-for-diffusion/derivative-uniform-distribution-480.webp 480w,/assets/img/langevin-sampling-for-diffusion/derivative-uniform-distribution-800.webp 800w,/assets/img/langevin-sampling-for-diffusion/derivative-uniform-distribution-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/langevin-sampling-for-diffusion/derivative-uniform-distribution.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Derivative of Uniform Distribution" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> The derivative of the uniform distribution is zero everywhere inside the interval, requiring boundary "walls" to keep samples within the valid range. </div> <p>However, this flat derivative could lead samples to drift outside the valid range, so we need to put “walls” that keep us inside the sample space we’re looking for. These walls act like a restoring force that pushes samples back when they wander outside $[0, 1]$.</p> <p>Using this $F$ and the Gaussian distribution, we can generate samples from a uniform distribution. Then we apply the second step: threshold on multiples of $1/6$, and we have a generator of dice rolls built entirely from Langevin dynamics.</p> <div class="row justify-content-sm-center"> <div class="col-sm-10 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/langevin-sampling-for-diffusion/uniform-distribution-from-langevin-480.webp 480w,/assets/img/langevin-sampling-for-diffusion/uniform-distribution-from-langevin-800.webp 800w,/assets/img/langevin-sampling-for-diffusion/uniform-distribution-from-langevin-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/langevin-sampling-for-diffusion/uniform-distribution-from-langevin.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Uniform Distribution from Langevin Sampling" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Generating samples from a uniform distribution using Langevin dynamics, then thresholding to produce dice rolls. </div> <h2 id="a-langevin-approach-to-image-generation">A Langevin Approach to Image Generation</h2> <p>If we were to sample from the PDF $p_{\text{image}}(\mathbf{x})$, where the sample space spans the dimension of all pixels in an image, it becomes much harder to visualize all possible combinations of those pixels. We need some way to obtain the score function of that PDF.</p> <h2 id="visualizing-score-functions-in-higher-dimensions">Visualizing score functions in higher dimensions</h2> <p>The score function can be visualized as a Vector Field that points in the direction of quickest increase of the PDF, with the length of each vector indicating the magnitude (the closer to the peak, the smaller the vector).</p> <p>The major issue for our image PDF is that we don’t have an analytical expression for it:</p> \[\text{for } \mathbf{x} = \begin{bmatrix} x_1 \\ x_2 \\ \vdots \\ x_{1,000,000} \end{bmatrix}, \quad x_i \text{ is pixel } i \text{ of image } \mathbf{x}\] \[\begin{align*} p_{\text{images}}(\mathbf{x}) &amp;= \text{ ?} \\ F_{\text{images}}(\mathbf{x}) &amp;= \nabla_{\mathbf{x}} \log p_{\text{images}}(\mathbf{x}) = \text{ ?} \end{align*}\] <p>In the dice roll case, we <em>did</em> know the PDF of the uniform distribution, so we could compute $F$ by just reasoning about the sample space. But for images, the problem is much harder. However, there’s one advantage we do have: a collection of millions of images from the internet — which are samples from this very PDF.</p> <h2 id="diffusion-models-estimate-unknown-score-functions-from-existing-samples">Diffusion Models Estimate Unknown Score Functions from Existing Samples</h2> <p>A Diffusion Model learns the direction that points toward the closest cluster of good images (with a nuance I’ll address in the final section) — in other words, the score function we’ve been talking about.</p> <p>This connects our Langevin Sampling approach with image generation. If we have the trained model providing the score function and this sampling technique, we can generate plausible images.</p> <h2 id="why-add-more-noise-in-the-denoising-process">Why Add More Noise in the Denoising Process?</h2> <p>Similarly to the $p_{\text{twopeaks}}(x)$ example, if we don’t add noise during the iterative sampling, we get a blurry mess — which could be seen as the average of many images rather than a true sample from the distribution. The noise contributes to two things:</p> <ol> <li><strong>Diversity</strong>: it ensures the generated samples reflect the full probability distribution rather than collapsing to a single mode.</li> <li><strong>Exploration</strong>: it helps escape local optima, combating the short-sightedness of pure gradient ascent.</li> </ol> <h2 id="noise-as-part-of-a-team">Noise as Part of a Team</h2> <p>The diffusion process and the noise can be seen as a two-person team: one logical, one creative. The work of both allows us to get realistic images. The purely logical component can’t generate a realistic image on its own — it needs randomness.</p> <p>This is worth contrasting with Generative Adversarial Networks (GANs), which suffered from a problem known as <em>mode collapse</em>, where outputs became far less diverse than the true distribution.</p> <h2 id="parallels-with-stochastic-gradient-descent">Parallels with Stochastic Gradient Descent</h2> <p>Stochastic Gradient Descent does something remarkably similar. Instead of finding a peak (like Langevin Sampling), it finds a minimum — but the principle is the same. The “noise” in SGD isn’t added explicitly; it’s a natural consequence of using mini-batches of data, which makes the gradient inherently noisy. This method works incredibly well and is the foundation of deep learning during training. Langevin Sampling just extends this same idea of productive randomness to test time.</p> <h2 id="final-nuances">Final Nuances</h2> <p>There are a few important details I glossed over for the sake of clarity:</p> <ul> <li> <p><strong>Annealed Langevin Sampling</strong>: The model actually learns the score of the <em>noised</em> distribution at each noise level. Why? Because if we were to start from a noise sample and try to follow the score of the true (un-noised) distribution, it would be too sparse — you’d get lost. Instead, we add noise to give the score function meaningful signal, and then gradually reduce the noise level, annealing from high to low noise during sampling.</p> </li> <li> <p><strong>Noise prediction ≈ Score estimation</strong>: During diffusion model training, the model learns to predict the added noise, and it’s not immediately obvious how this connects to the score function. Proving this equivalence is a topic for another post, but the short version is that predicting the noise is mathematically equivalent to estimating the score.</p> </li> <li> <p><strong>Why does Langevin Sampling work at all?</strong> This question leans into statistical physics and stochastic calculus. The theoretical justification comes from the theory of Langevin dynamics and its connection to the Fokker-Planck equation. If you’re curious, that’s where to dig deeper.</p> </li> </ul> <h2 id="glossary">Glossary</h2> <p><strong>Probability Mass Function (PMF):</strong> For a discrete random variable $X$, the PMF $p_X(x)$ gives the probability that $X$ takes the value $x$:</p> \[p_X(x) = P(X = x)\] <p>where $\sum_{x \in \mathcal{X}} p_X(x) = 1$.</p> <p><strong>Probability Density Function (PDF):</strong> For a continuous random variable $X$, the PDF $f_X(x)$ describes the probability density at $x$. The probability that $X$ falls in an interval $[a, b]$ is:</p> \[P(a \leq X \leq b) = \int_a^b f_X(x) \, dx\] <p>where $\int_{-\infty}^{\infty} f_X(x) \, dx = 1$.</p> <p><strong>Score Function:</strong> The gradient of the log-probability density, $\nabla_x \log p(x)$. It points in the direction that most quickly increases the probability density.</p> <p><strong>Langevin Sampling:</strong> An iterative algorithm that generates samples from a target distribution using only the score function and Gaussian noise.</p> <p><strong>Mode Collapse:</strong> A failure mode in GANs where the generator produces only a small subset of the possible outputs, lacking diversity.</p>]]></content><author><name>Ricardo Huaman</name></author><category term="diffusion"/><category term="models,"/><category term="langevin"/><category term="sampling,"/><category term="generative"/><category term="models"/><summary type="html"><![CDATA[An overview of Langevin Sampling for Diffusion Models]]></summary></entry><entry><title type="html">My Journey Learning Japanese</title><link href="https://rotvie.github.io/blog/2025/my-journey-learning-japanese/" rel="alternate" type="text/html" title="My Journey Learning Japanese"/><published>2025-11-29T00:00:00+00:00</published><updated>2025-11-29T00:00:00+00:00</updated><id>https://rotvie.github.io/blog/2025/my-journey-learning-japanese</id><content type="html" xml:base="https://rotvie.github.io/blog/2025/my-journey-learning-japanese/"><![CDATA[<h2 id="my-childhood">My Childhood</h2> <p>Looking back, I think I’ve always had an interest in languages. The idea of communicating in ways that other people couldn’t easily understand fascinated me. It all started when I was around 8 years old. I remember learning an alternative symbol for each letter of the alphabet and writing secret messages with those. It was quite fun, but it had the shortcoming that I was the only one who knew that code, so I couldn’t share my ideas with others.</p> <p>A few years later, when I was 12, I learned about Braille and finally understood what the symbols on the buttons of elevators meant. But most importantly, I appreciated the cleverness in such a system that allowed blind people to read. During the following years, I realized that learning a new language would be much more useful, so I set that as my goal.</p> <h2 id="getting-started">Getting Started</h2> <p>There were many options. I tried some of them using Duolingo and similar apps. I studied French, Portuguese, German, Italian, Esperanto, among other languages, but of all of them, Japanese was the one that really got my attention. It seemed complex, yet Japanese culture was so widespread that it shouldn’t be too hard, I thought—how naive I was then.</p> <p>In December 2014, I decided that I was going to master Japanese. At first, I started reading a book of common phrases in Japanese with its translation in Spanish. However, that alone wasn’t going to take me far, so I began looking for a concise grammar book. That’s when I came across <a href="https://www.guidetojapanese.org/grammar_guide.pdf">Japanese Grammar Guide</a> by Tae Kim.</p> <h2 id="first-attempt">First Attempt</h2> <p>English was a compulsory subject in my school, so I knew the basics, but I was still far from being fluent. Nonetheless, if I wanted to read that Tae Kim textbook, I had to force myself to read English.</p> <p>In 2015, after learning Hiragana with the help of mnemonics, I began reading the book. During that process, I had to look up many English words and sentence structures that now are obvious to me. I think the most noticeable change then was how much my English improved—more than my Japanese, even.</p> <p>Around that time, I also liked to use a website called <a href="https://en.wikipedia.org/wiki/Lang-8.com">Lang-8</a> where you could post paragraphs in the language you were learning for native speakers to correct. I also bought <a href="https://en.wikipedia.org/wiki/Remembering_the_Kanji">Remembering the Kanji</a> by James Heisig, but never really read it because of bad reviews I saw immediately afterwards (I kind of regret not reading it then).</p> <h2 id="getting-off-track">Getting Off Track</h2> <p>I didn’t finish reading Japanese Grammar Guide. I got sidetracked by English content on the internet, but at least I had the general idea of how Japanese worked. A few years passed and my English understanding improved gradually. I knew some Japanese already too, so I thought I would get better if I kept reviewing a little here and there. That never happened.</p> <p>I still had the intention of becoming fluent in another language. Japanese was at the top of my list, but I wasn’t sure how to tackle a language that looked so different from the others. At the end of 2019, Matt vs Japan Youtube channel finally clarified the strategy for me.</p> <h2 id="finding-direction">Finding Direction</h2> <p>The approach to language learning that he used to learn Japanese made sense to me. He said that the most important thing was to focus on getting input in the target language through reading and listening. I reflected upon the way I learned English and everything clicked for me—immersion in the language is crucial.</p> <p>What about Kanji? There are thousands of those Chinese characters. Matt’s solution was the book Remembering the Kanji along with a software for spaced repetition called <a href="https://apps.ankiweb.net/">Anki</a>.</p> <h2 id="second-attempt">Second Attempt</h2> <p>Once again, I was all-in with Japanese, but this time I had a plan. The first few months of 2020, I was learning to write Kanji at a rate of 25 new characters every day using mnemonics and Anki. When I got to a thousand, I stopped to prioritize watching Japanese content. Maybe I should have continued until I was done with the 2,200 Kanji in the book, but I was excited to start watching actual Japanese and expand my vocabulary.</p> <p>After a couple of months of consuming content in Japanese, I returned to Kanji and finished the book at last.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/my-journey-learning-japanese/my-japanese-handwriting-480.webp 480w,/assets/img/my-journey-learning-japanese/my-japanese-handwriting-800.webp 800w,/assets/img/my-journey-learning-japanese/my-japanese-handwriting-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/my-journey-learning-japanese/my-japanese-handwriting.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <h2 id="where-im-at">Where I’m At</h2> <p>In July 2022 I passed the <a href="https://www.linkedin.com/posts/ricardo-jesus-huaman-kemper_certificado-jlpt-n2-activity-6988881391144812544-pfup?utm_source=share&amp;utm_medium=member_desktop&amp;rcm=ACoAACZjz_wBVCnBVO8PKF0qAZFPBmbqVUDky4I">JLPT N2</a> exam after doing immersion for around 2 years at that point. I’ve also created a <a href="https://github.com/Rotvie/ajatt">repository</a> where I share the tools and resources I’ve found most useful for language learning. Now I’m just keeping the routine of learning new words through a technique called <em>Sentence Mining</em> and trying to immerse myself in the language as much as possible, although it’s been harder lately because of other priorities.</p> <p>I omitted tons of details in my story, mainly because I don’t remember them well or because they’re kind of irrelevant. I wanted to write this down before I forget even further.</p> <h2 id="final-thoughts">Final Thoughts</h2> <p>I think learning Japanese has been and continues to be an enriching experience that has shaped who I am now. I encourage everyone who is thinking about learning a new language to give it a try—not just for the sake of a better resume or a job opportunity. Don’t get me wrong, I recognize those are great things too. Just don’t underestimate the impact it can have on one’s understanding of different cultures and perspectives.</p> <p>Best of luck with your studies!</p>]]></content><author><name>Ricardo Huaman</name></author><category term="japanese,"/><category term="language"/><category term="learning"/><summary type="html"><![CDATA[My personal journey learning Japanese]]></summary></entry><entry><title type="html">Setting Up and Running microROS on the Raspberry Pi Pico</title><link href="https://rotvie.github.io/blog/2023/uros/" rel="alternate" type="text/html" title="Setting Up and Running microROS on the Raspberry Pi Pico"/><published>2023-06-08T00:00:00+00:00</published><updated>2023-06-08T00:00:00+00:00</updated><id>https://rotvie.github.io/blog/2023/uros</id><content type="html" xml:base="https://rotvie.github.io/blog/2023/uros/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>In the rapidly evolving world of robotics and embedded systems, microROS is a game-changer. It brings the capabilities of ROS (Robot Operating System) 2 to microcontrollers, facilitating the development of robust and interactive systems. The Raspberry Pi Pico, with its powerful RP2040 chip, provides an excellent platform for such applications.</p> <p>This tutorial will guide you through setting up and running microROS on the Raspberry Pi Pico. By the end of this tutorial, you’ll be equipped to leverage the power of microROS on your Raspberry Pi Pico <d-cite key="RoboFoundry_2022"></d-cite> <d-cite key="ubuntuGettingStarted"></d-cite> <d-cite key="micro-ros-agent-ros"></d-cite> <d-cite key="micro-ros-agent-github"></d-cite>.</p> <p>For the corresponding <strong>YouTube tutorial</strong>, click <a href="https://youtu.be/_4aLzvG6dfw?si=oyEmNZsfEt8569Nv">here</a>.</p> <hr/> <h2 id="setting-up-the-environment">Setting up the Environment</h2> <p>Install the necessary dependencies with the following command:</p> <figure class="highlight"><pre><code class="language-shell" data-lang="shell"><span class="nb">sudo </span>apt <span class="nb">install </span>cmake g++ gcc-arm-none-eabi doxygen libnewlib-arm-none-eabi git python3 build-essential</code></pre></figure> <p>Configure the environment:</p> <figure class="highlight"><pre><code class="language-shell" data-lang="shell"><span class="nb">echo</span> <span class="s2">"export PICO_TOOLCHAIN_PATH=..."</span> <span class="o">&gt;&gt;</span> ~/.bashrc
<span class="nb">echo</span> <span class="s2">"export PICO_SDK_PATH=</span><span class="nv">$HOME</span><span class="s2">/micro_ros_ws/src/pico-sdk"</span> <span class="o">&gt;&gt;</span> ~/.bashrc
<span class="nb">source</span> ~/.bashrc</code></pre></figure> <h2 id="pico-sdk-and-microros">Pico-SDK and microROS</h2> <p>Create the workspace and clone the required repositories:</p> <figure class="highlight"><pre><code class="language-shell" data-lang="shell"><span class="nb">mkdir</span> <span class="nt">-p</span> ~/micro_ros_ws/src
<span class="nb">cd</span> ~/micro_ros_ws/src
git clone <span class="nt">--recurse-submodules</span> https://github.com/raspberrypi/pico-sdk.git
git clone https://github.com/Rotvie/micro_ros_raspberrypi_pico_sdk.git</code></pre></figure> <h2 id="compilation-and-loading">Compilation and Loading</h2> <p>Compile the microROS code to generate the UF2 file, which will be loaded onto the Raspberry Pi Pico:</p> <figure class="highlight"><pre><code class="language-shell" data-lang="shell"><span class="nb">cd </span>micro_ros_raspberrypi_pico_sdk
<span class="nb">mkdir </span>build
<span class="nb">cd </span>build
cmake ..
make</code></pre></figure> <p>To load the UF2 file, connect the Pico while holding the BOOTSEL button, then run:</p> <figure class="highlight"><pre><code class="language-shell" data-lang="shell"><span class="nb">cp </span>build/pico_micro_ros_example.uf2 /media/<span class="nv">$USER</span>/RPI-RP2</code></pre></figure> <h2 id="configuring-the-microros-agent">Configuring the microROS agent</h2> <p>Install and configure the microROS agent:</p> <figure class="highlight"><pre><code class="language-shell" data-lang="shell"><span class="nb">sudo </span>snap <span class="nb">install </span>micro-ros-agent
<span class="nb">sudo </span>snap <span class="nb">set </span>core experimental.hotplug<span class="o">=</span><span class="nb">true
sudo </span>systemctl restart snapd
snap interface serial-port
snap connect micro-ros-agent:serial-port snapd:pico</code></pre></figure> <h2 id="running-the-microros-example-demo">Running the microROS example demo</h2> <p>To start the microROS node, run:</p> <figure class="highlight"><pre><code class="language-shell" data-lang="shell"><span class="nb">sudo </span>micro-ros-agent serial <span class="nt">--dev</span> /dev/ttyACM0 <span class="nv">baudrate</span><span class="o">=</span>115200</code></pre></figure> <p>Check the available topics with:</p> <figure class="highlight"><pre><code class="language-shell" data-lang="shell">ros2 topic list</code></pre></figure> <h2 id="connecting-the-pico-with-a-raspberry-pi-instead-of-a-pc">Connecting the Pico with a Raspberry Pi Instead of a PC</h2> <p>For a Raspberry Pi setup, follow the instructions up to the Configuring the microROS agent section. However, if you encounter an error when installing the micro-ros-agent with snap (e.g., <code class="language-plaintext highlighter-rouge">error: snap "micro-ros-agent" is not available on stable for this architecture (arm64)</code>), follow these steps:</p> <ol> <li>Set up a new ROS 2 workspace for the microROS agent:</li> </ol> <figure class="highlight"><pre><code class="language-shell" data-lang="shell"><span class="nb">source</span> /opt/ros/<span class="nv">$ROS_DISTRO</span>/setup.bash
<span class="nb">mkdir </span>uros_ws <span class="o">&amp;&amp;</span> <span class="nb">cd </span>uros_ws
git clone <span class="nt">-b</span> <span class="nv">$ROS_DISTRO</span> https://github.com/micro-ROS/micro_ros_setup.git src/micro_ros_setup
rosdep update <span class="o">&amp;&amp;</span> rosdep <span class="nb">install</span> <span class="nt">--from-paths</span> src <span class="nt">--ignore-src</span> <span class="nt">-y</span>
colcon build
<span class="nb">source install</span>/local_setup.bash</code></pre></figure> <ol> <li>Build the microROS Agent manually:</li> </ol> <figure class="highlight"><pre><code class="language-shell" data-lang="shell">ros2 run micro_ros_setup create_agent_ws.sh
ros2 run micro_ros_setup build_agent.sh
<span class="nb">source install</span>/local_setup.sh</code></pre></figure> <ol> <li>Start the microROS Agent:</li> </ol> <figure class="highlight"><pre><code class="language-shell" data-lang="shell">ros2 run micro_ros_agent micro_ros_agent <span class="nt">--dev</span> /dev/ttyACM0 <span class="nv">baudrate</span><span class="o">=</span>115200</code></pre></figure>]]></content><author><name>Ricardo Huaman</name></author><category term="ros,"/><category term="microROS,"/><category term="microcontroller,"/><category term="robotics"/><summary type="html"><![CDATA[A tutorial on how to get microROS and ROS2 working together]]></summary></entry></feed>